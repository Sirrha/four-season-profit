# System-inventar — Four Season AS

**Last updated**: 2026-06-04, after Phase 2f.1b (shadow writes re-throw + surface failures — no more silent mirror divergence).

- **Siste endring**: Phase 2f.1b (2026-06-04) — **shadow-write hardening (surface-only)**. Tre shadow-helpers gjorde tidligere `try{ ... }catch(e){ console.error(...); return null }` — caller fikk null signal på feil, mirror kunne stille drive ut av sync med purchases mens UI rapporterte success. Etter 2f.1a er ALLE leser-paths på mirror, så stille shadow-feil = stille datapunkt-tap i UI. **Fem chunks**: (A) `shadowCreateExpense` catch (linje ~6928): `return null` → `throw e` (beholder `console.error` for debug, men retter caller-signal). (B) `shadowUpdateExpense` catch (linje ~6954): samme — `return null` → `throw e`. (C) `shadowDeleteExpense` catch (linje ~6969): la til `throw e;` (helper hadde tidligere ingen return — kun void). (D) `saveInnkjop` linje ~2056: SHADOW-kallet wrapped i ny `try{...}catch(e){showToast(...)}` (men ikke hele funksjonen — success-toast linje 2022 + closeModal 2023 fyrer fortsatt FØR shadow som idag, så success-path er pixel-identisk). Feil-toast: `'Faktura ble lagret, men synk til oversikten feilet. Prøv å åpne/lagre på nytt, eller kontakt support hvis den ikke vises.','err'` — post-facto inkonsistens-advarsel (faktura er commited til purchases, men ikke til mirror; user må gjøre noe). (E) `deleteInnkjop` linje 1776: one-liner hadde ingen try/catch — wrapped body i `try{...}catch(e){showToast('Kunne ikke slette helt. Prøv igjen.','err')}`. **Eksplisitt UNTOUCHED**: de 5 modal-flyt-writers (`saveEditHeader` 2334, `saveEditLine` 2420, `deleteInvoiceLine` 2462, `saveNewLine` 2543, `deletePurchase` 2245) — disse har allerede outer `try{...}catch(e){showToast('Kunne ikke ...: '+e.message,'err')}` som nå AUTOMATISK fanger thrown shadow-error via samme catch. Null code-endring nødvendig i de 5; harden propagerer gratis. **Success-path pixel-identisk**: ingen reordering av success-toasts, closeModal eller showInvoiceDetail. Bare failure-path skifter: tidligere stille success-illusjon → nå synlig error-toast som faktisk speiler tilstanden. **Sub-concern dokumentert**: `saveEditLine` har post-shadow product-buyEks/salgEks-sync (linje 2422-2444) som nå skippes om shadow throws — recoverable inkonsistens, ny save på samme produkt re-syncer; akseptert. **Kjent residual**: en feilet CREATE-shadow under 2f.1b lar fakturaen være i purchases men usynlig i mirror-fed UI; ingen auto-recovery i mellomperioden. Løses i 2f.1d når create blir single mirror-write (ingen shadow lenger; expense IS the primary). Flag-gating (WRITE_TO_PURCHASES) og write-cut er 2f.1c/d-territorium. **Sanity-check reads UNCHANGED**: getPurchaseInvoiceById=13, getPurchaseInvoiceMirrors=27, dbAll('purchases')=1 (5113 backfill), dbFind('purchases'=2 (7081 docstring + 7130 fallback) — eksakt samme tall som etter 2f.1a.
- **Tidligere milestone**: Phase 2f.1a (2026-06-04) — **9 siste purchases-leser-callsites migrert**: 8 write-precondition `dbFind('purchases', id)` → `getPurchaseInvoiceById(id)` (linje 2240 deletePurchase + 2254 showEditHeaderForm + 2311 saveEditHeader + 2343 showEditLineForm + 2400 saveEditLine + 2453 deleteInvoiceLine + 2471 showAddLineForm + 2521 saveNewLine) pluss 1 dup-check `dbAll('purchases').filter` → `getPurchaseInvoiceMirrors().filter` (linje 1967 saveInnkjop). **Adapter urørt** — alle 11 helper-felter (id, dato, forfallsdato, fakturanr, lev, leverandorId, lines, notes, originalFiles, betaltVedLevering, betalingsmetode) dekker både skjema-prefill og shadow-spread-round-trip. **Round-trip verifisert**: `buildExpenseDocFromPurchase` leser eksakt de 11 feltene helperen returnerer; **`opprettet` brukes ALDRI av builderen** (mirror's `createdAt` settes alltid frisk på CREATE, og `shadowUpdateExpense` bevarer `existing.createdAt` på UPDATE-pathen — `p.opprettet`-fraværet i helper-returnen er strukturelt inert). For de 4 spread-writers (`saveEditHeader`/`saveEditLine`/`saveNewLine`/`deleteInvoiceLine`) er `{...p, ...other, id}` byte-ekvivalent for hver av de 11 feltene som builderen faktisk leser. **Null-handling identisk**: `dbFind` returnerer `undefined`, helper returnerer `null` på miss → begge trigger `if(!p)return;` likt; ingen semantisk drift. **Default-disambiguering**: lines 2240/2254/2311 hadde identisk linjetekst (`const p=dbFind('purchases',id);if(!p)return;`), lines 2343/2400/2453/2471/2521 hadde annen identisk tekst (`const p=dbFind('purchases',purchaseId);if(!p)return;`); 8 separate `Edit`-kall med funksjonsdeklarasjon som disambiguerings-kontekst (samme strategi som 2f.0f). **Eksplisitt IKKE rørt** (per scope): linje 7130 (`getPurchaseInvoiceById` defensive fallback `return dbFind('purchases',id)||null`), linje 7081 (stale `getPurchaseInvoiceMirrors` docstring-tekst — refresh i 2f.1b), linje 5113 (`backfillPurchasesToExpenses` Phase 2b authoritative read — sirkulær avhengighet hvis migrert, permanent vestige). **Pre-commit grep verifiserte**: dbFind('purchases' = eksakt 2 (7081 docstring + 7130 fallback), dbAll('purchases') = eksakt 1 (5113 backfill). **Status etter 2f.1a**: alle USER-FACING purchases-leser-paths migrert til mirror. Writes (`tenantCol('purchases').doc(id).set/update/delete`, `dbDelete('purchases', id)`, `dbUpdate('purchases', id, ...)`) **fortsatt uendret** — Phase 2f.1b handler write-cutover + WRITE_TO_PURCHASES-flag + shadow-error-hardening. Idag: shadow-write holder mirror synkronisert mens reads er på mirror-first; null behavior-endring forventet pixel-wise.
- **Tidligere milestone**: Phase 2f.0h (2026-06-03) — **åttende og siste commit av reader-fanout-serien (2f.0a..2f.0h). Reader-fanout under Phase 2f-paraplyen er nå COMPLETE**. Tre scanner-readers migrert: dup-check linje 5783 + `findForrigeBuy` linje 6324 + `findForrigeInnhold` linje 6346. **Adapter urørt** — `fakturanr`, `lev`, `dato`, `opprettet`, `lines` (med rik line-subfield-set: `productId`, `prisPerEnhetEks`, `vatRate`, `innholdPerKolli`, `vektPerKolli`, `prisPerKolliInkl`) alle allerede aliased. **Tre swaps**: (A) Scanner dup-check linje 5783 (i `runAIScan`-faktura-branchen) — early-abort hvis AI-parset `fakturanr + lev` matcher eksisterende. **`opprettet` (lagt til adapteren i 2f.0a) nå faktisk konsumert** av confirm-dialogens `'Lagret: '+fmtDate((dup.opprettet||'').slice(0,10))`-linje — opprinnelig motivasjon for 2f.0a-tillegget realisert her. (B) `findForrigeBuy` linje 6324 — scanner pre-fill-helper som returnerer `{dato, prEnhetEks}` for forrige innkjøp av et produkt før `currentDate`. (C) `findForrigeInnhold` linje 6346 — samme shape, returnerer `innhold||vektPerKolli`. Begge sorterer dato-desc og itererer for første line-match på `productId`. **Implementasjons-trikk**: Chunks 2+3 hadde **byte-identisk linje**, pre-grep verifiserte eksakt 2 hits ved 6324+6346 (ingen 3. kollisjon noen steder i filen) → ett `replace_all:true`-kall byttet begge i ett trekk; Chunk 1 (multi-line `.filter` start) editet med distinkt kontekst. **🔑 Load-bearing for 2f.1**: alle 3 sites må lese mirror så de fungerer korrekt etter write-cutover. Pre-2f.1 ser shadow-write gjør at både purchases og expenses har samme set; post-2f.1 vil nye fakturaer kun finnes i expenses, og hvis disse readers fortsatt så purchases ville dup-detection og prev-buy-prefill stille slutte å se nye fakturaer. Migrert nå som forward-compat (idag null adferds-endring, men korrekt under 2f.1 cutover). **KJENT kosmetisk drift**: scanner dup-dialogens "Lagret:"-linje viser **backfill-dato 2026-05-31** for de 38 historiske mirrors i stedet for original `opprettet` (adapter aliaser `opprettet:e.createdAt`, og for backfilled mirrors er `createdAt = backfill-tidspunkt`). Akseptert i 2f.0a som "kosmetisk fidelitet-drift" — edge-case påvirker bare re-skan-av-historisk-faktura-scenarioet, dup-detection selv (key=fakturanr+lev) er upåvirket. **Status etter 2f.0h**: 21 av 22 reader-sites migrert. **Eneste gjenstående reader på purchases**: saveInnkjop write-precondition dup-check linje 1967 (følger 2f.1 write-cutover — bruker samme `fakturanr+lev`-dedup-key som linje 5783, så semantikken er identisk). **Migrasjons-vestige**: `backfillPurchasesToExpenses` linje 5113 forblir på `dbAll('purchases')` permanent — sentinel-gated Phase 2b-migrasjon som leser purchases autoritativt for å BYGGE expenses, sirkulær avhengighet hvis rutet gjennom adapter. **Post-2f.0h dbAll('purchases')-count = eksakt 2** (1967 + 5113). Reader-paraplyen Phase 2f.0a..2f.0h totalt: ~21 reader-sites på 8 commits, hver med Chrome Claude pixel-gate, alle PASSERTE. Klar for Phase 2f.1 (write-cutover).
- **Tidligere milestone**: Phase 2f.0g (2026-06-03) — **syvende commit av reader-fanout-serien**: `exportCSV('purchases')`-branchen linje 4909 migrert. **Adapter urørt** — alle 18 CSV-kolonner mapper til aliased felter (`dato`, `forfallsdato`, `fakturanr`, `lev` topp-nivå pluss alle line-subfields `productId`/`lineKind`/`ekskluderFraBeregning`/`antall`/`vektPerKolli`/`totalVekt`/`prisPerKolliInkl`/`totalInkl`/`totalEks`/`prPerEnhetInkl`/`prPerEnhetEks`/`prod.navn`/`prod.salgEks` via `dbFind('products', l.productId)`). **`notes`, `originalFiles`, `betaltVedLevering`, `betalingsmetode` ikke emittert** — disse er kun i `getPurchaseInvoiceById` (single-record helper for modal-chain), ikke i list-adapteren. **Per-cell byte-fidelitet verifisert**: falsy-mappings (`p.forfallsdato=undefined → mirror.dueDate=null → adapter null → ${null||''} → ''` osv.) gir identiske bytes. Linje 93273's `lev=''` håndteres identisk pre/post. **Option B — deterministisk sort tilført**: nytt `.slice().sort((a,b)=>(a.dato||'').localeCompare(b.dato||'')||(a.fakturanr||'').localeCompare(b.fakturanr||''))` foran `.forEach` — sorterer dato-asc med fakturanr-asc som tiebreak. **Rasjonale**: pre-2f.0g brukte CSV `dbAll('purchases').forEach(...)` med Firestore-snapshot-rekkefølge (purchases-collection, doc-ID-leksikografisk). Post-2f.0g ville en ren swap brukt expenses-collection-snapshot-rekkefølge (blanding av `mirror-<purchaseId>`-prefixed backfilled-IDs og auto-genererte shadow-IDs) — rad-content byte-likt per celle, men rad-rekkefølge ville skiftet pga ulik snapshot-ordering mellom kolleksjonene. Med sort: rad-rekkefølge er deterministisk uavhengig av kilde-snapshot, hver eksport fra dette punkt fremover er byte-identisk for samme datasett. **Trade-off eksplisitt akseptert av Herish**: første post-2f.0g-eksport matcher hverken siste pre-2f.0g-eksport (rad-rekkefølge skifter til sorted) eller fremtidige rene unsorted exports — det er en one-time engangs-reshuffle for permanent diffability fremover. Alternativ Option C (skip 0g, behold på purchases) ble forkastet pga silent-data-loss-risiko etter 2f.1 (purchases slutter å akkumulere). **Pre-edit unik-grep verifisert**: `dbAll('purchases').forEach(p=>{` traff eksakt 1 site (linje 4909) — de andre 5 gjenstående bruker `const purchases=...`-form eller `.filter`/`.slice().sort()`-suffiks, ingen kollisjon. **Status etter 2f.0g**: 18 av 22 reader-sites migrert. 4 gjenstår: 3 Scanner (2f.0h), 1 saveInnkjop dup-check (følger 2f.1). Backfill-migrasjon linje 5113 migrerer ALDRI — det er Phase 2b's authoritative purchases-read som bygger expenses, sirkulær avhengighet hvis flyttet til adapter.
- **Tidligere milestone**: Phase 2f.0f (2026-06-03) — **sjette commit av reader-fanout-serien, største batch til nå**: fem `dbAll('purchases')` → `getPurchaseInvoiceMirrors()`-swaps i Rapporter-tab-funksjonene. **Adapter urørt** — `dato`, `lines` (via `calcPurchaseStats`), pluss `id`/`fakturanr`/`lev` for rapFakturaer er allerede aliased. `opprettet` ikke brukt av noen rapport. **Fem swaps**: (A) `rapFakturaer` linje 4576 — `dbAll('purchases').slice().sort(...)` swap (UNIK linje med chained sort, ingen kollisjon). 📋 Fakturaer-tab: liste alle fakturaer date-desc, totals tBuyInkl + tBuyEks + gmPanel(tGm, totGmPct, totPaaslag). (B) `rapKategorier` linje 4609 — 📂 Kategorier-tab. **Kategori-attribuering bekreftet på product-nivå**, ikke purchase-nivå: indre løkke `for(const l of p.lines)` → `prod=dbFind('products',l.productId)` → `k=prod.kategori` → bucket på `cats[k]`. Ingen `p.kategori`-felt finnes eller leses. (C) `rapDag` linje 4654 — 📅 Per dag-tab, top 30 dager date-desc. (D) `rapUkeVsUke` linje 4758 — 📆 Uke vs Uke-tab, top 8 uker week-desc med trend-pil mot forrige uke. (E) `rapGraf` linje 4819 — 📈 Graf-tab, 30-dagers bar-chart (innkjøp + GM kr per dag) + bunn-liste topp 10 nyligste. **⚠️ Kritisk safety-disambiguering**: chunks 2-5 hadde **byte-identisk linje** `const purchases=dbAll('purchases');`, og pre-grep avslørte at samme linje ALSO finnes på linje 5113 i `backfillPurchasesToExpenses` (Phase 2b-migrasjon — out of scope). Et `replace_all` ville stilltiende ha bytet 5113 også og trukket migrasjonen inn i denne commiten — potensielt sirkulær avhengighet (migrasjonen leser purchases for å BYGGE expenses; hvis den leser expenses returnert via adapteren får vi en chicken-and-egg). Strategien: 5 individuelle `Edit`-kall med funksjonsdeklarasjon som disambigueringskontekst (`function rapFakturaer(){\n  const purchases=...`, osv.). Linje 5113 forblir urørt. Pre-commit-grep verifiserte eksakt 6 `dbAll('purchases')` gjenstår (1967 saveInnkjop dup-check, 4909 exportCSV, 5113 backfill, 5783 scanner dup, 6324 findForrigeBuy, 6346 findForrigeInnhold) — alle resterende sites + migrasjonen. **Numerisk integritet**: alle 5 rapport-aggregater henter fra enten `calcPurchaseStats(p)` (leser kun `p.lines`, bevart literalt på mirror) eller per-linje-iterasjon `for(const l of p.lines)` med `prod.kategori`-lookup på product-siden (uendret). Identiske 38 inputs → byte-identiske summer over alle tabs. **Status etter 2f.0f**: 17 av 22 reader-sites migrert (Oversikt + LowGM + 3 Salg + 3 Svinn + 1 Betalinger + 3 Leverandører + 5 Rapporter). 5 gjenstår: 1 CSV (2f.0g), 3 Scanner (2f.0h), 1 saveInnkjop dup-check (følger med 2f.1 write-cutover). Pluss 8 write-precondition + 7 writes i 2f.1.
- **Tidligere milestone**: Phase 2f.0e (2026-06-03) — **femte commit av reader-fanout-serien**. Leverandører-siden migrert: tre `dbAll('purchases')` → `getPurchaseInvoiceMirrors()` swaps. **Adapter urørt** — alle felter (`lev` og `leverandorId`) er allerede aliased. **Tre swaps**: (A) `renderLeverandorer` linje 3297 — top-of-page unmigrated-names scan `[...new Set(...).map(p=>p.lev).filter(x=>x&&x.trim())]`. Henter unike free-text supplier-navn fra alle fakturaer for å vise "⚠️ N navn fra produkter og innkjøp er ikke i registeret"-warningen. (B) `renderLeverandorer` linje 3328 — per-supplier purchases-count-loop inni `for(const l of all)`. Filter `.filter(p=>p.leverandorId===l.id||normName(p.lev)===normName(l.navn)||(l.aliaser||[]).some(a=>normName(a)===normName(p.lev)))`, telles bare som `.length` for "N innkjøp"-badgen per leverandør-rad. Three-way fingerprint-match (structured ID, normalized free-text, alias). (C) `migrateLeverandorer` linje 3418 — identisk struktur som (A), kalles fra "📦 Opprett N leverandører nå"-knappen for å bulk-opprette nye leverandør-records. **Skriver bare til `leverandorer`-kolleksjonen** (`dbAdd('leverandorer', ...)` linje 3425) — aldri til purchases, så ingen write-precondition-konflikt. **Optimisering — `replace_all`-trick for Chunks 1+3**: linje 3297 og 3418 hadde **byte-identisk** tekst, så én `Edit replace_all:true`-kall byttet begge i ett trekk; en separat `Edit replace_all:false` for Chunk 2's distinkte filter-linje. 3 distinkte linje-endringer i totalt 2 tool-kall — verifisert via grep-count fra 14 → 17 (+3). **Field-fidelitet verifisert**: writer linje ~6967 har `supplierName:p.lev||''` (free-text bevart verbatim, falsy → tom string), linje ~6981 har `leverandorId:p.leverandorId||''` (structured ID bevart). Adapter aliaser begge tilbake. Per-supplier-count fra de 38 mirrors produserer identisk resultat fra tre-veis-fingerprint-matchen. **Invoice 93273** (paid-at-delivery med tom `lev`-felt): adfærd identisk — `supplierName:''` på mirror, adapter returnerer `lev:''`, `.filter(x=>x&&x.trim())` dropper den fra både Callsite A og C. Hverken bidrar til `purchSuppliers`-Set'en eller til unmigrated-listen, pre- eller post-migrasjon. **`renderLeverandorer` per-supplier-loop performance**: ~16 suppliers × 38 mirrors = ~608 sammenligninger per render. Neglisjerbart, samme kompleksitet pre/post (adapter returnerer fresh `.map()`-array av samme størrelse). **Status etter 2f.0e**: 12 av 22 reader-sites migrert (Oversikt + LowGM + 3 Salg + 3 Svinn + 1 Betalinger + 3 Leverandører). 10 gjenstår: 5 Rapporter (2f.0f), 1 CSV (2f.0g), 3 Scanner (2f.0h), pluss 8 write-precondition + saveInnkjop dup-check (2f.1). Write-paths urørt.
- **Tidligere milestone**: Phase 2f.0d (2026-06-03) — **fjerde commit av reader-fanout-serien, minste til nå**: enkelt `dbAll('purchases')` → `getPurchaseInvoiceMirrors()`-swap i `renderBetPhase2Invoices` linje 3550 (Ny betaling-modalens phase-2 unpaid-invoice-picker). **Adapter urørt** — alle felter (`leverandorId` for supplier-filter, `lines` via calcPurchaseStats, `id` for `getInvoicePaymentStatus(p.id)` + `betSelectedInvoices`-Set, `dato` for sort, `fakturanr` for display) er allerede aliased. **Audit av hele Betalinger-blokken** (linje 3439-3700) bekreftet at `renderBetPhase2Invoices` er **eneste** purchases-leser blant 9 Betalinger-funksjoner — `renderBetalinger`, `openBetalingModal`, `onBetLevChange`, `goToBetPhase2/1`, `toggleBetInvoice`, `updateBetLinkStatus`, `saveBetaling` rører ikke purchases. **CRITICAL: `leverandorId`-aliasing** verifisert: adapter linje ~7091 har `leverandorId:e.leverandorId`, og mirror writer `buildExpenseDocFromPurchase` linje ~6982 preserverer `leverandorId:p.leverandorId||''` med samme navn (i tillegg til engelsk `supplierId` som speilfelt). Supplier-filter `p.leverandorId===levId` produserer identisk sub-set som før. **Chain-verifikasjon for `getInvoicePaymentStatus(p.id)`**: kallet får ekte purchase-ID via adapter's `id:e.linkedPurchaseId`-alias. `getInvoicePaymentStatus` (linje ~1217, migrated 2e) kaller `getPurchaseInvoiceById(invoiceId)` → finner mirror via `linkedPurchaseId===id`. For paid-at-delivery-fakturaer (101273, 142960, 51871, 93273, 141924, 141694, 141553) short-circuit'er på `purchase.betaltVedLevering===true` (preserved av 2e-prep). For non-paid-at-delivery filtreres `bankTransactions.linkedeFakturaer.includes(invoiceId)` — `invoiceId` er ekte purchase-ID, samme som lagret. **Round-trip-integritet end-to-end**: `betSelectedInvoices`-Set lagrer `p.id` (ekte purchase-ID), `saveBetaling` skriver disse til `bankTransaction.linkedeFakturaer[]`, fremtidige `getInvoicePaymentStatus`-kall matcher dem mot samme ID-form. Ingen "mirror-<id>"-forvirring mulig. **Status etter 2f.0d**: 9 av 22 reader-sites migrert (Oversikt + LowGM + 3 Salg + 3 Svinn + 1 Betalinger). 13 gjenstår: 3 Leverandører (2f.0e), 5 Rapporter (2f.0f), 1 CSV (2f.0g), 3 Scanner (2f.0h). Write-paths urørt — flippes i 2f.1.
- **Tidligere milestone**: Phase 2f.0c (2026-06-03) — **tredje commit av reader-fanout-serien**. Svinn-siden migrert: tre `dbAll('purchases')` → `getPurchaseInvoiceMirrors()` swaps. **Adapter urørt** — alle felter (`id`, `fakturanr`, `dato`, `lev`, `lines`) er allerede aliased fra 2f.0a/b's grunnsett. **Tre swaps**: (A) `renderSvinn` linje 2982 — Svinn-side hovedrenderer. To downstream-accesses: `purchases.find(f=>f.id===s.fakturaId)` ved 2989 (linked-faktura lookup), `faktura.fakturanr` display ved 2995 (grønt badge "· Faktura<nr>"). Begge ✅ aliased. (B) `openSvinnModal` linje 3010 — `ov-svinn`-modal som åpnes fra Svinn-FAB (bound i FAB-map linje 1394 + 1399, live UI). Date-desc-sort + slice(0,20), bygger `<option>`-liste med `p.id` som value, `${fmtDate(p.dato)} · ${p.fakturanr||'Uten nr'} · ${p.lev||'–'}` som label. Alle ✅ aliased. (C) `onSvinnPrdChange` linje 3020 — bound på `#svi-prd onchange` linje 667. Når valgt produkt er frukt (`p.kategori==='frukt'`), filtrerer alle fakturaer på `(pu.lines||[]).some(l=>l.productId===pid)` (any line for valgt produkt), sorterer dato-desc, pre-fyller `svi-faktura` med `recent[0].id`. Alle ✅ aliased. **Round-trip-integritet**: `svinn.fakturaId ↔ purchase.id` bevart fordi adapter's `id:e.linkedPurchaseId` = ekte purchase-ID (samme verdi som `saveSvinn` skriver fra `svi-faktura`-options). `renderSvinn`'s lookup ved linje 2989 finner riktig faktura. **Frukt ±14-dagers auto-attribuering**: bekreftet UTE av scope for disse tre callsites — den logikken lever i `calcInvoiceReelStats` (linje ~1192, auto-filter 1201-1205) som allerede er migrert via `getPurchaseInvoiceById` i Phase 2e. `onSvinnPrdChange`'s frukt-handling er separate "most-recent invoice"-pre-fill uten dato-vindu. **Status etter 2f.0c**: 8 av 22 reader-sites migrert (Oversikt + LowGM + 3 Salg + 3 Svinn). 14 gjenstår: 1 Betalinger (2f.0d), 3 Leverandører (2f.0e), 5 Rapporter (2f.0f), 1 CSV (2f.0g), 3 Scanner (2f.0h). Write-paths urørt — flippes i 2f.1.
- **Tidligere milestone**: Phase 2f.0b (2026-06-02) — **andre commit av reader-fanout-serien**. Salg-siden migrert: tre `dbAll('purchases')` → `getPurchaseInvoiceMirrors()` swaps. **Adapter urørt** — alle felter (`dato`, `lines` via calcPurchaseStats, `id`, `fakturanr`, `lev`) er allerede aliased fra 2f.0a's grunnsett. **Tre swaps**: (A) `renderSalg` linje 2563 — Salg-side hovedrenderer. 7 distinkte downstream-accesses verifisert: periodPurchases-filter på `p.dato` (2572), calcPurchaseStats(p) summert til periodForvSalgEks/periodBuyEks (2577), full-purchases-iteration ved `salgPeriod==='alle'` (2599), per-dagsalg-rad dayPurchases-filter (2662) + Forv kr-sum (2664), Detaljerte salg-lookup `purchases.find(p=>p.id===s.fakturaId)` (2698), `faktura.fakturanr` display (2701). Alle ✅ aliased. (B) `showDagsalgDetail` linje 2827 — Z-rapport detail modal. Filter `p.dato===d.dato` + `calcPurchaseStats(p).forvSalgEks`-sum for "Sammenligning mot innkjøp samme dag"-panel. Alle ✅ aliased. (C) `openSalgModal` linje 2928 — `ov-salg`-modal for detaljerte salg. Grep bekreftet **eksakt 0 callere** i hele filen (kun funksjonsdefinisjonen treffes) — bekrefter system-inventory.md linje 225 om at modalen er dead UI (ikke koblet til FAB eller hdr-knapp). Migrert likevel for konsistens (per Herish' eksplisitte valg over "retire entirely"). Pretty sortering + slice(0,20) bevart; alias-shape returnerer fresh `.map()`-array så `.slice()` er teknisk redundant men kost null. **Round-trip-integritet**: `s.fakturaId ↔ p.id`-chain bevart fordi adapter's `id:e.linkedPurchaseId` = ekte purchase-ID (samme verdi som `saveSalg` skriver fra `salg-faktura`-options). `renderSalg`'s lookup ved linje 2698 finner riktig faktura uansett `openSalgModal`-write-side eller manuell-skriving fra fortiden. **Status etter 2f.0b**: 5 av 22 reader-sites migrert (Oversikt + LowGM + 3 Salg). 17 gjenstår: 3 Svinn (2f.0c), 1 Betalinger (2f.0d), 3 Leverandører (2f.0e), 5 Rapporter (2f.0f), 1 CSV (2f.0g), 3 Scanner (2f.0h), pluss 8 write-precondition-callsites + saveInnkjop dup-check (alle i 2f.1). Skanner-readers bruker fortsatt `dbAll('purchases')`, så ny faktura ville fortsatt være synlig i scanner-dup-deteksjon — ingen visuell regresjon forventet. **`openSalgModal` retirement-decision**: utsatt til separat fremtidig cleanup-commit; Herish flagget eksplisitt at "NOT retire — that's a separate future decision."
- **Tidligere milestone**: Phase 2f.0a (2026-06-02) — **første commit av reader-fanout-serien under Phase 2f-paraplyen** (8 commits planlagt: 2f.0a..2f.0h reader-migrasjoner, så 2f.1 write-cutover, evt. 2f.2 sanity sweep). 2f recon avdekket 22 ikke-migrerte reader-sites på tvers av Oversikt, Salg, Svinn, Betalinger, Leverandører, Rapporter, CSV-eksport og AI-skanner — alle bruker fortsatt `dbAll('purchases')` selv etter Phase 2e closing av Innkjøp/Utgifter/modal-paths. Stopper Phase 2f.1 writes UTEN først å migrere disse leserne, og nye fakturaer ville stille forsvinne fra halve appen (silent data invisibility — D8-brudd). 2f.0a er første steg av staged fanout. **Tre endringer i denne commiten**: (A) `getPurchaseInvoiceMirrors`-adapter utvidet med ett nytt felt: `opprettet:e.createdAt` (linje ~7090, slottet ved siden av `dato`/`forfallsdato` for date-felt-cluster). Docstring oppdatert fra "No new fields, no computed properties — pure rename" til "Pure rename — no computed properties. Field set grows as more pages join the fanout" + 4-linjers note om opprettet's fidelitet på de 38 backfilled mirrors (deres `createdAt` = backfill-dato 2026-05-31, ikke original purchase opprettet — kosmetisk drift akseptert, eneste konsumer er AI-skanner dup-prompt linje ~5797 som er edge-case for re-skan av historisk faktura). (B) `renderOversikt` linje 1413: `dbAll('purchases')` → `getPurchaseInvoiceMirrors()`. Field-walkthrough bekreftet: alle access (dato sort/filter på linje 1415/1416/1482, calcPurchaseStats på 1418/1420/1488, lev/fakturanr/id/dato/lines.length på 1489-1493) er aliased. Ingen access bruker `opprettet` her — sort uses `dato`. (C) `showLowGmDiagnostics` linje 1565: samme swap. Field-walkthrough bekreftet: `pu.lines.some(l=>l.productId===p.id)` på 1575 (lines er literal copy), sort på `dato` 1576, `lastInv.lev/fakturanr/dato/id` 1578-1579. `showInvoiceDetail`-callsite på 1491 (Oversikt) og 1579 (LowGM) bruker `p.id`/`lastInv.id` som = `linkedPurchaseId` via adapter-alias = ekte purchase-ID → modal's `getPurchaseInvoiceById` finner mirror og åpner korrekt (samme chain validert i Phase 2e). **Strategiendring fra `getUtgifterInvoices`-rename til shared adapter** (Phase 2d) gjør at denne fanouten bruker samme funksjon på tvers av seks pages — ikke parallel-helpers per page. Hver fanout-commit forventes pixel-identisk via Chrome Claude. **Out of scope for 2f.0a**: 20 gjenværende reader-sites (Salg/Svinn/Betalinger/Leverandører/Rapporter/CSV/Scanner) — kommer i 2f.0b..2f.0h. Write-paths urørt — flippes i 2f.1.
- **Tidligere milestone**: Phase 2e proper (2026-06-02) — **andre commit av 2e-prep + 2e-paret** (Path γ split): den faktiske read-cutoveren for modal og payment-status. Etter at 2e-prep gjorde mirror-dokumentet komplett (alle 38 mirrors bærer nå `betaltVedLevering` + `betalingsmetode` direkte), kunne 2e proper bygge en ekte mirror-first reader. **Ny helper `getPurchaseInvoiceById(id)`** (linje ~7095, co-located rett etter `getPurchaseInvoiceMirrors`): single-record adapter med 11 aliased felter (`id` ← `linkedPurchaseId`, `dato` ← `invoiceDate`, `forfallsdato` ← `dueDate`, `fakturanr` ← `invoiceNumber`, `lev` ← `supplierName`, `leverandorId`, `lines`, `notes`, `originalFiles`, `betaltVedLevering`, `betalingsmetode` — alle nødvendige for de fire READ-callsites identifisert i recon). Mirror-first via `dbAll('expenses').find(e=>e.expenseKind==='purchase_invoice'&&e.linkedPurchaseId===id)`, returnerer purchase-shaped objekt hvis funnet. **Defensive fallback**: hvis mirror mangler (skal ikke skje i steady state etter 2e-prep — kun belt-and-braces for Phase 2f write-path-migrasjon eller pre-backfill new tenants), warn-once via `window._missingMirrorWarned` Set (én console.warn per id, ingen spam), så `dbFind('purchases', id)||null`. Vurdert å droppe fallback for renere "9 → 8 callsites"-tall i grep — **avvist** fordi defense-in-depth her er gratis og arkitekturisk korrekt. **Fire READ-callsite-swaps** (én-linje hver, ren identifier-bytte): `calcInvoiceReelStats` linje ~1192, `getInvoicePaymentStatus` linje ~1217, `showInvoiceDetail` linje ~2062, `openOriginalFile` linje ~2221. **Chain-verifikasjon for `getInvoicePaymentStatus`**: short-circuit-branchen leser `purchase.betaltVedLevering===true` (strict boolean fra helper), returnerer `paidAtDelivery:true, paymentMethod:purchase.betalingsmetode||null` (string|null fra helper) — bytefor-byte identisk for paid-at-delivery-fakturaer. Non-paid-at-delivery-branch leser `purchase.forfallsdato` (aliased fra `dueDate`), beregner `isOverdue` mot `today()` — identisk. **Phase 2c/2d-konsumenter** (Innkjøp-chip, Utgifter-pill, modal-pill) leser `pay.paymentMethod`/`pay.paidAtDelivery`/`pay.status`/`pay.isOverdue` — ingen shape-endring, ingen UI-touch. **Subtilitet om `forfallsdato`**: writer ved linje ~6970 setter `dueDate:p.forfallsdato||null` FØR paid-at-delivery if-block; `saveInnkjop`/`saveEditHeader` skriver `effectiveForfall=paidAtDelivery?dato:forfall` til purchase, så purchase.forfallsdato matcher dato når paid-at-delivery — mirror.dueDate speiler det riktig. **DO NOT swap (Phase 2f-territorium)**: de 8 WRITE-precondition-callsites (`deletePurchase` linje 2239, `showEditHeaderForm` 2253, `saveEditHeader` 2310, `showEditLineForm` 2342, `saveEditLine` 2399, `deleteInvoiceLine` 2452, `showAddLineForm` 2470, `saveNewLine` 2520) — disse leser fra purchases for å skrive til purchases; flippes når Phase 2f flytter writes. **Performance**: helper er O(n) over ~38 mirrors per oppslag, `renderInnkjop` med 38 rader gjør ~1444 sammenligninger per render — neglisjerbart, ingen caching lagt til. **Sammen utgjør 2e-prep + 2e Phase 2e som helhet**: mirror er nå source-of-truth for alle Innkjøp/Utgifter READ-paths; runtime-avhengighet på purchases bare ved (a) de 8 WRITE-callsites og (b) defensive fallback i helperen (skal aldri fyre). Total fix-størrelse: ~32 helper-linjer + 4 callsite-swaps + inventory.
- **Tidligere milestone**: Phase 2e-prep (2026-06-02) — **første commit av 2e-prep + 2e-paret** (Path γ split): infrastruktur-forberedelse. `buildExpenseDocFromPurchase` utvidet med `betaltVedLevering` + `betalingsmetode` (additive), backfill-migrasjon `backfillMirrorPaymentFields()` (sentinel-gated, Phase 2b-fix-disciplin), boot-call etter dedupeMigratedExpenseMirrors. Resultat: alle 38 mirrors bærer disse to feltene direkte, slik at 2e proper kunne bygge ekte mirror-first reader uten å miste "✓ Betalt · Bankaxept"-chip-segmentet. — infrastruktur-forberedelse for Phase 2e (modal + payment-status read cutover). Pre-2e-prep var mirror-dokumentet **lossy**: `buildExpenseDocFromPurchase` (linje ~6963) bakte paid-at-delivery-effekten inn i `paidDate` + `paidDateSource='manual'`, men kopierte ikke `betaltVedLevering` (boolean) eller `betalingsmetode` (string|null) direkte. `betalingsmetode` ble fullt tapt. Resultat: en hypotetisk Phase 2e mirror-first-reader ville mistet "✓ Betalt · Bankaxept"-chip-segmentet på Innkjøp-liste, BETALINGER-panel i modal, og payment-pill i Utgifter — visuell degradering, Sirrha D8-brudd. **Constraint clarification fra Herish**: "DO NOT touch write paths" i 2e refererte til **user-facing write paths** (saveInnkjop, saveEditHeader, deleteInnkjop, edit/delete line flows). **Infrastructure write paths** (mirror writer, backfill migrations) er fair game når nødvendig for cutover. 2e-prep faller i infra-kategorien. **Endringer**: (A) `buildExpenseDocFromPurchase` utvidet med to nye additive felter i initializer-blokken (rett etter `paidAmountMatched`-clusteret): `betaltVedLevering:p.betaltVedLevering===true` (normalisert strict boolean) og `betalingsmetode:p.betalingsmetode||null` (normalisert string|null). Eksisterende paid-at-delivery `if`-block på linje ~6989-6993 urørt — derived `paidDate`/`paidDateSource` brukes fortsatt av existing-consumers. Nye felter er pure-additive read-target for 2e proper. (B) Ny migrasjon **`backfillMirrorPaymentFields()`** (linje ~5218) — strukturelt identisk til `dedupeMigratedExpenseMirrors` (Phase 2b-fix-disciplin): sentinel-gated via `_meta/migrations.mirrorPaymentFieldsBackfilledAt`, authoritative reads fra Firestore (`tenantCol('expenses').get()` + `tenantCol('purchases').get()`, ikke `dbAll(...)`, fordi `showApp` fyrer før snapshots lander), per-record `try/catch`, sentinel kun skrevet når `failed===0` (retry-friendly). **Per-record desired-state-skip** (`m.betaltVedLevering===desired.betaltVedLevering && m.betalingsmetode===desired.betalingsmetode → skip`) gjør partial-success-retries idempotent — completed docs re-skrives ikke. Logger `[backfill-payment-fields] updated: N, skipped: M, failed: K`. Hard exception → toast "Backfill av betalingsfelter feilet — prøver igjen" + console.error. (C) Boot-call i `showApp()` linje ~1079, rett etter `dedupeMigratedExpenseMirrors()` og før login/role-branch — fire-and-forget, idempotent. **Expected first-boot console** etter deploy: `[backfill-payment-fields] updated: 38, skipped: 0, failed: 0` (alle 38 pre-2e-prep mirrors mangler `betaltVedLevering`-feltet helt → JS strict equality `undefined===false` → false → alle skrives med eksplisitte defaults). Andre-boot og senere: `[backfill-payment-fields] already complete, skipping`. **Hva 2e-prep IKKE gjør** (følger i Commit 2e proper): adapteren `getPurchaseInvoiceById(id)` + swap av 4 READ-callsites (`calcInvoiceReelStats` linje 1192, `getInvoicePaymentStatus` linje 1217, `showInvoiceDetail` linje 2062, `openOriginalFile` linje 2221). User-facing write paths (saveInnkjop, saveEditHeader, etc.) urørt — Phase 2f-territorium. Ingen UI-endring forventet etter 2e-prep — ingen leser ennå de nye mirror-feltene. Total fix-størrelse: ~49 kode-linjer + ~13 kommentar-linjer (eks. inventory).
- **Tidligere milestone**: Phase 2d (2026-06-02) — D8 adapter cutover for **Innkjøp**-siden: leser nå fra `expenses`-kolleksjonen filtrert på `expenseKind==='purchase_invoice'`, via samme aliasing-adapter som Phase 2c. **Per Sirrha D8: null visuell endring** — pixel-identisk før/etter. Editable modal (Rediger/Slett/edit-knapper) urørt — `showInvoiceDetail` leser fortsatt fra `purchases` via `dbFind('purchases', id)` og får ekte purchase-ID gjennom adapterens `id:e.linkedPurchaseId`-alias. **Adapter renamet `getUtgifterInvoices` → `getPurchaseInvoiceMirrors`** (linje ~7003) — det er nå en delt adapter for alle konsumenter som trenger `purchase_invoice`-mirrors aliased til purchase-shape (Utgifter + Innkjøp); navnet "Utgifter" var misvisende etter Phase 2d-cutover. Docstring oppdatert tilsvarende. Phase 2c's tre callsites (linje ~3050, ~3139, ~3211) oppdatert til nytt navn — ren identifier-swap, ingen logikkendring. **Innkjøp callsite-swap** (linje ~1717): `dbAll('purchases').slice().sort(...)` → `getPurchaseInvoiceMirrors().slice().sort(...)` — `.slice()` beholdt selv om adapter returnerer fresh `.map()`-array (defensiv, kost null). **Shape-gap håndtering**: mirror-dokumentet preserverer **ikke** `betaltVedLevering` eller `betalingsmetode` direkte (`buildExpenseDocFromPurchase` baker bare paid-at-delivery-effekten inn i `paidDate` + `paidDateSource='manual'`; `betalingsmetode` går fullt tapt). Innkjøp-render brukte begge ved linje 1746-1747 for "✓ Betalt · Bankaxept"-chip. Valgt **Option W** (over Option X derived eller Option Y mirror-writer-fix): bytt fra `p.betaltVedLevering`/`p.betalingsmetode` til `pay.paidAtDelivery`/`pay.paymentMethod` — `getInvoicePaymentStatus(p.id)` (allerede kalt linje 1742) short-circuit'er på `purchase.betaltVedLevering===true` og returnerer disse to feltene fra ekte purchase-doc. Adapterens `id:linkedPurchaseId`-alias sørger for at `dbFind('purchases', invoiceId)` inni `getInvoicePaymentStatus` finner riktig rad. Output byte-identisk; struktur **renere** (payment-state single-source via `getInvoicePaymentStatus`, ikke dual-sourced fra `p.*` + `pay.*`). 2-linjers forklarende kommentar over `if(pay.paidAtDelivery===true)` så fremtidig leser ikke "fixer" det tilbake. **Out of scope for 2d** (eksplisitt): `exportCSV('purchases')` urørt (leser fortsatt purchases direkte — eksport er datakontrakt, ikke render); `buildExpenseDocFromPurchase` urørt (write-path, "DO NOT modify"); modal-internals i `showInvoiceDetail` urørt (continuity i editable-flyt); operating-expense rendering urørt. **Phase 2d-spesifikk verifikasjons-invariant for Chrome Claude**: bekrefte at "✓ Betalt · Bankaxept"-chip fortsatt vises korrekt på en paid-at-delivery-faktura (sannsynlig Tenza 142960 eller annen 2025-faktura) — Phase 2c-BEFORE-screenshot dekket ikke dette tilfellet. Total fix-størrelse: ~9 endrede kode-linjer + ~9 kommentar-linjer.
- **Tidligere milestone**: Phase 2c (2026-06-02) — D8 adapter cutover: Utgifter-siden leser nå fra `expenses`-kolleksjonen i stedet for `purchases`. **Etter Phase 2d**: adapter renamet `getUtgifterInvoices` → `getPurchaseInvoiceMirrors`. Hvis du leser eldre git-loggen og lurer på `getUtgifterInvoices`: det er samme funksjon, omdøpt 2026-06-02 i Phase 2d-commiten. **Per Sirrha D8: null visuell endring** — pixel-identisk før/etter. Ny adapter-funksjon `getUtgifterInvoices()` (linje ~7003, co-located rett etter `buildExpenseDocFromPurchase`) filtrerer `dbAll('expenses')` på `expenseKind==='purchase_invoice'` og **mapper hver mirror-doc tilbake til purchase-shape via field-aliasing** (Path A valgt over per-site renames): `id:e.linkedPurchaseId`, `dato:e.invoiceDate`, `forfallsdato:e.dueDate`, `fakturanr:e.invoiceNumber`, `lev:e.supplierName`, `leverandorId:e.leverandorId`, `lines:e.lines`. **Hvorfor aliasing**: `buildExpenseDocFromPurchase` (linje ~6959) renamet de fleste top-level-feltene (dato→invoiceDate, forfallsdato→dueDate, lev→supplierName, fakturanr→invoiceNumber) — en ren passthrough-adapter ville derfor produsert undefined-felter i renderer. Adapter konsentrerer oversettelsen ett sted slik at render-bodies står helt urørt. **Kritisk: `id:e.linkedPurchaseId`** — gjør at downstream-kall `getInvoicePaymentStatus(p.id)` og `showInvoiceDetail(p.id, …)` (begge `dbFind('purchases', id)` internt) får ekte purchase-ID, ikke `'mirror-<purchaseId>'`. Tre callsite-swaps (én linje hver): `renderUtgifter` linje ~3050, `renderUtgifterShelves` linje ~3139, `renderUtgifterShelf` linje ~3211 — alle bare `dbAll('purchases')` → `getUtgifterInvoices()`. **Null endringer i render-bodies**, null endring på `showInvoiceDetail`-callsite linje ~3264 (adapter aliaser `id` til `linkedPurchaseId`, så `${p.id}` i templatet er allerede purchase-ID). **Bevart bakoverkompatibelt**: `calcPurchaseStats(p)` får literal kopi av `e.lines` → identiske totals (incl. ekskluder/pant-flagg på linjenivå); `getInvoicePaymentStatus` leser ekte `betaltVedLevering` via purchase-lookup; sort-comparator `(b.dato||'').localeCompare(a.dato||'')` på "Siste aktivitet" virker fordi `dato` er aliased. Total fix-størrelse: ~17 nye linjer (adapter) + 3 endrede linjer (callsites). **DO NOT touch i denne fasen**: Innkjøp-siden (Phase 2d), `showInvoiceDetail`-modalen (modal leser fortsatt fra purchases — bevart kontinuitet i read-only-visning), shadow-write/backfill/dedup (korrekt som-shipped i a5ed256), operating-expense rendering (rent/utility/equipment — separat sub-step 4+).
- **Tidligere milestone**: Phase 2b-fix (2026-06-01) — kritisk data-integritets-fix etter Phase 2b (74afaba) speilet 38 purchases til 558 `migrated_from_purchase` expense-docs pga. en showApp()-re-entry-loop. **Tre koordinerte fixes**: (1) **Listener boot-loop** (linje ~1043): `let listenersReady=0` (modul-level counter med `>=`-sjekk som ble true permanent) erstattet med `const firstSnapshotSeen={}` per-collection-first-snapshot-flag — `showApp()` fyrer nå nøyaktig én gang når første snapshot fra ALLE 14 listeners har landet, og påfølgende snapshots re-renderer kun aktiv side. Reset i `startListeners()` via `Object.keys(firstSnapshotSeen).forEach(k=>delete ...)` for tenant-switch-scenarios. Pluss `let _appShown=false` belt-and-braces guard som første linje i `showApp()`. Felles re-render-call kollapset (var tidligere kalt to ganger på non-first-snapshot-ticks). (2) **Deterministisk mirror-ID i backfill** (linje ~5119): `dbAdd('expenses',exp)` (auto-ID — sårbart for konkurrens) erstattet med `tenantCol('expenses').doc('mirror-'+p.id).set({...exp,id:'mirror-'+p.id})` — strukturelt idempotent: samtidige kall skriver til samme doc, last-write-wins, ingen duplikater mulig. **Endrer IKKE `shadowCreateExpense`s `dbAdd`-mønster** — shadow-write trigges av user-actions (én save = ett kall), ikke av snapshots, så samme exposure finnes ikke der. (3) **One-shot cleanup-migrasjon `dedupeMigratedExpenseMirrors()`** (linje ~5137) i MIGRATIONS-blokken: leser alle `expenses`-docs fra Firestore (authoritative — defense-in-depth mot subtile Fix-1-bugs), sletter ALLE `source==='migrated_from_purchase'`-docs, gjenoppretter nøyaktig én per `linkedPurchaseId` med deterministisk `'mirror-<purchaseId>'`-ID. Sentinel-gated via `_meta/migrations.expenseMirrorsDedupedAt`. Henter purchases direkte fra Firestore (`tenantCol('purchases').get()`) i stedet for `dbAll('purchases')` — samme defense-in-depth-prinsipp som Phase 2b-backfill. Per-record `try/catch`. Sentinel skrives kun når `failed===0`. Boot-kall i `showApp()` etter `backfillPurchasesToExpenses()`. **End state**: 38 purchases ↔ 38 deterministisk-ID'd expense-mirrors (`mirror-<purchaseId>`). Total fix-størrelse: ~70 nye linjer på tvers av tre seksjoner.
- **Tidligere milestone**: Phase 2b (2026-06-01) — one-shot backfill-migrasjon. Ny funksjon `backfillPurchasesToExpenses()` (linje ~5088, rett etter `migratePaymentsToBankTransactions` i MIGRATIONS-blokken) som speiler eksisterende `purchases` (pre-shadow-write, før commit bc8e13a 2026-05-26) inn i `expenses`-kolleksjonen. Sentinel-gated via `_meta/migrations.purchasesBackfilledAt` (ISO-timestamp). Boot-kall i `showApp()` linje 1070 — fire-and-forget, idempotent. **Race-safe lookup**: leser eksisterende mirror-IDs direkte fra Firestore (`tenantCol('expenses').get()` → Set av `linkedPurchaseId`), ikke via `LOCAL.expenses`, fordi `showApp()` fyrer før første snapshot fra `startListeners()` lander. Per-purchase `try/catch` slik at én feil ikke blokkerer de andre. **Sentinel skrives kun når `failed===0`** — hvis noen records feiler, holdes døra åpen for retry på neste boot (vellykkede records hoppes da over via mirroredPurchaseIds-sjekk). Bruker `buildExpenseDocFromPurchase(p, 'migrated_from_purchase')` (gyldig EXPENSE_SOURCE-verdi; spec sa 'migration' men det er ikke i enum — `'migrated_from_purchase'` er den eksplisitte enum-verdien for nettopp denne use casen). Logger `[backfill] created: N skipped: M failed: K` (eller `[backfill] already complete, skipping` ved senere boots). Ingen UI-endringer, ingen modifikasjon av eksisterende funksjoner annet enn ett-linjes boot-kall i `showApp()`, ingen `recurring_templates`-berøring, og operating expenses (`source:'manual'`) fra step 3 røres ikke.
- **Tidligere milestone**: Phase 2a step 3 (2026-05-31) — manuell driftsutgift-entry. To nye modaler: **`ov-utgifter-actions`** (FAB bottom-sheet på Utgifter — `📄 Skann faktura` / `✏️ Legg til manuelt` / `📥 Åpne innboks`) og **`ov-ny-utgift`** (selve skjemaet — kind/leverandør/datoer/total/MVA/kategori/periode/notater/bilag). FAB-map (linje 1296) fikk `utgifter:openUtgifterFabActionSheet`-entry (tidligere udekket → FAB var no-op på Utgifter). Nye funksjoner i nytt block etter `ensureExpenseTaxonomySeed`: `openUtgifterFabActionSheet`, `openNyUtgiftForm`, `onNyUtgiftKindChange`, `onNyUtgiftCategoryChange`, `onNyUtgiftFilesChosen`, `saveNyUtgift`. Skjemaet bygger doc via `buildExpenseDocFromOperatingForm`, leverandør-autocomplete via HTML5 `<datalist>` over `LOCAL.leverandorer` (eksakt case-insensitive name-match resolver `supplierId`, ellers free-text-only), kategori-dropdown sourcer fra `LOCAL.expense_taxonomy[kind].categories`, periode-rad vises kun for `KINDS_WITH_PERIOD`. For periode-kinds auto-settes `confirmedOneOff:true` (recurring template-UI er senere). Filopplasting via ny Storage V1-helper **`uploadExpenseFile(expenseId,file)`** (parallell til `uploadInvoiceFile` på linje 888) som lagrer under `tenants/{TID}/expenses/{id}/{ts-filename}` med samme metadata-shape som `originalFiles[]` forventer. **Save-semantikk**: prevaliderer kun supplier+date+total (toast + behold modal hvis disse mangler); resten faller gjennom til `accountantStatus='missing_info'` med toast "Utgift lagret — mangler: X" — `missing_info` er førsteklasses tilstand, ikke feil. Skriver kun til `expenses` (ingen `purchases`-mirror — operating expenses lever bare i expenses-kolleksjonen). Doc-ID reserveres via `tenantCol('expenses').doc()` før upload slik at filsti kan bruke endelig ID, og doc skrives én gang med `originalFiles` allerede i plass (unngår mellomtilstand der listeneren ser `missing_info` som umiddelbart flippes til `ready`).

Dette dokumentet kartlegger hva som faktisk finnes i `index.html` (produksjon, sormena.no). Mål: at Soren (chat) og Claude Code skal jobbe ut fra samme bilde av systemet og slippe å duplikere arbeid eller spørre om ting som allerede er bygget.

- **Filer kartlagt**: `index.html` (~6086 linjer)
- **Siste store endring**: Bankaxept Level 1 (2026-05-23) — paid-at-delivery støtte for fakturaer som allerede er gjort opp ved levering (Tenza, small Coca-Cola, kontant Royal). Bakgrunn: 2026-05-21 traff Herish to konkrete smerter — (1) Royal manual entry-form spør om forfallsdato som er irrelevant for kontant-betalt Royal, (2) Tenza-fakturaer viser Bankaxept-bevis i bunntekst men AI ignorerer det. Prinsipp (lock): payment state må matche virkeligheten; hvis fakturaen er allerede betalt, skal Sormena vite det. Tre betalings-scenarier i Four Season-reality: (a) Faktura med betalingsfrist (Vatan, Royal-faktura) — håndtert i dag via V0.5 forfallsdato; (b) Faktura allerede betalt ved levering (Tenza, small Coca-Cola, kontant Royal) — denne fixen; (c) Faktura uten klar frist — ambivalent, mindre presserende. Level 2 (Bank Scanner Session 2-integrasjon, automatisk matching mot bank-transaksjoner) **utsatt**. Out of scope V1.0: CSV Betalingsstatus-kolonne, scanner-side override-toggle (V1 lener seg på manual form-checkbox), bulk migration script for eksisterende fakturaer. **Nye datafelter** på `purchases` (begge optional, eksisterende fakturaer uten dem oppfører seg som før): `betaltVedLevering:boolean` og `betalingsmetode:'bankaxept'|'kontant'|'vipps'|'kort'|'annet'|null`. Når true, settes `forfallsdato=fakturaDato` (per AI-prompt-konvensjon — gjør opp samme dag som levering). Ny modul-wide hjelper `paymentMethodLabel(m)` + const `PAYMENT_METHOD_LABELS` (linje ~1188, rett etter `getInvoicePaymentStatus`) — single source of truth for display-labels brukt på 5 ulike sites. **Endringer på tvers av 9 subsystemer**: (A) **AI-prompt** (linje 4901 schema + ny BETALT VED LEVERING-regel mellom L4931 og forfallsdato-blokken): schema utvidet med `betaltVedLevering` + `betalingsmetode`. Ny deteksjons-regel krever BÅDE metode-nøkkelord (BankAxept/EMV/Kontant/Cash/Vipps/Visa/Mastercard/masked card) OG autoriserings-nøkkelord (Autorisert/Authorised/Betalt/PAID/Innbetalt/Trans-ID/Godkjent) i samme dokument — svakt signal → null (ikke false-positive på "Betalingsvilkår 14 dager"-boilerplate). Eksplisitte Tenza/Royal/Vipps-eksempler. Når true: AI setter forfallsdato=fakturadato selv om "Betalingsfrist N dager" finnes. Math V1's MATH-VERIFY, rabatt, totalLinje, pant, selgesSom, innholdPerKolli, forfallsdato V0.5-regler urørt. (B) **Scanner UI header** (linje 5452 i showScanResults): grønn pill "✓ Betalt ved levering · Bankaxept" erstatter "Forfall: ..."-segmentet når `data.betaltVedLevering===true`. **`confirmScanToInnkjop`** (linje 6086 + L6143-6157) propagerer state til manual form-widgets via direkte checkbox-set + dropdown-set + `setPaidAtDeliveryUI('inn')`-kall, både første-gang-path og re-open-path. (C) **Manual form** (linje 631-657 i `ov-innkjop`): ny checkbox "Allerede betalt ved levering" + skjult betalingsmetode-dropdown over forfallsdato-raden (eksisterende `<div class="fr">` fikk `id="inn-forfall-row"`). Ny delt hjelper `setPaidAtDeliveryUI(prefix)` (linje ~1629, rett før onInnkjopLevChange) toggler display + visuelt klart forfall-input når sjekket — fungerer for både `prefix='inn'` og `prefix='edith'`. (D) **Edit-header modal** (linje 2021-2024 i `showEditHeaderForm`): mirror checkbox + dropdown, default-state hentet inline fra `p.betaltVedLevering`-flag via `${...}` template, samme `setPaidAtDeliveryUI('edith')` onchange-handler. (E) **Innkjøp list** (linje 1584-1591 i `renderInnkjop`): paid-at-delivery → grønn `✓ Betalt · Bankaxept`-chip i stedet for forfall-chip (belt + suspenders gate). (F) **Detail view** (linje 1838-1855 i `showInvoiceDetail` + L1877-1879 BETALINGER panel): ny `isPaidAtDelivery`-const øverst, gate `forfallSegment` + `overdueBanner` på `!isPaidAtDelivery`. BETALINGER panel: når `isPaidAtDelivery && linkedPayments.length===0` rendres grønn sub-line "✓ Betalt ved levering · Metode · kr X · DD.MM.YYYY" i stedet for "Ingen betalinger lenket ennå"-placeholder. Telleren "0 betalinger" justert til "1 betaling (ved levering)" for samme tilfelle. Status pill ("✓ Betalt") beholdes generisk per Q3-avgjørelse. (G) **`getInvoicePaymentStatus`** (linje 1169-1185): top-of-function short-circuit på `purchase?.betaltVedLevering===true` returnerer `{paid:true, paymentIds:[], paidAmount:total, status:'paid', outstanding:0, isOverdue:false, daysOverdue:0, paidAtDelivery:true, paymentMethod:purchase.betalingsmetode||null}`. Legacy body byte-for-byte uendret etter short-circuit. Kaskaderer korrekt gjennom `renderBetPhase2Invoices` (L3041 `status!=='paid'` ekskluderer cash-paid fra picker), pill-rendering, og forfall-isOverdue (status='paid' short-circuit'er L1182). (H) **`saveInnkjop`** (linje 1761-1812) og **`saveEditHeader`** (linje 2037-2065): leser checkbox + dropdown, beregner `effectiveForfall=paidAtDelivery?dato:forfall`, persisterer begge nye felter (eksplisitt false/null når ikke sjekket — slik at edit fra paid→unpaid faktisk overskriver). (I) **`openInnkjopModal`** (linje 1616-1632): reset av nye widgets + invoke toggle helper for konsistent default-state. Math V1, Matcher V1.1, Pant V1, Ekskluder, Session 1.6, autoDetectScanRule, MATH-VERIFY-regel **alle urørt**.
- **Tidligere milestone**: Math Verification V1 (2026-05-23) — bundled fix for tre relaterte feil avdekket av Sunrise 94-linjers faktura 2026-05-20: (1) AI tolket eks-MVA-priser som inkl-MVA — alle 15%-linjer ble delt på 1,15 feil (158,49 × 1,15 = 182,26 invoice-Delsum bevist); 25%-pall var korrekt → bugg-spesifikk for 15%-sats. Self-consistency defeats math cross-check fordi AIs `totalInkl` ble utledet fra samme feiltolkede linjer. (2) Auto-regel-tekst manglet på 94-linjers faktura (`innerText.includes('Auto-regel')→false`) — eksisterende `showScanResults` (linje 5405) hadde tre tekst-grener (`uklar`/`isClear`/best-guess) hvorav kun `isClear`-grenen sa "Auto-regel"; ble til "Ingen perfekt match" når avvik >5%. Toggle-knappene rendres alltid, men brukeren oppfattet ingen override-sti uten "Auto-regel"-anker. (3) Sum avviker sammenlignet App-Inkl mot AI-utledet faktura-Inkl → 13% falsk alarm når AIs `totalInkl` var feil. Prinsipp (locked 2026-05-20): fakturaens egen matematikk er invariant truth; AI må verifisere mot fakturatotalene; bruker må alltid kunne overstyre. Tre additivt-koordinerte endringer: **(A) AI-prompt schema utvidet** med `totalEks` (literal "Delsum eks. MVA"/"Sum eks"-celle, null hvis ikke synlig) + optional `mvaBreakdown:{"15":{grunnlag,mva},"25":{...}}` (samme shape som z-rapport på linje 4846). Ny MATH-VERIFY-regel etter eksisterende KONTROLL (linje 4907): krever at totalInkl/totalEks **alltid hentes fra printet celle** (aldri utledes), at AI **selv-sjekker** sum(linjer)≈totalEks innenfor 5 kr/0,5% og grunnlag×(1+S/100)≈grunnlag+mva pr sats innenfor 1 kr — **hvis ikke stemmer: revurder linjetolkningen før retur**. Eksplisitt Sunrise/AFFA/Tenza-eksempel om 15% MVA-felle. Rabatt/totalLinje/MVA/pant/selgesSom/innholdPerKolli/forfallsdato-regler urørt. **(B) Auto-regel label always-render** i `showScanResults` (linje 5427–5440): kollapset 3 tekstgrener til alltid å starte med "🧮 Auto-regel:" + severity-suffix — grønn `✓ (avvik N kr)` hvis isClear, gul `⚠️ (beste gjetning — avvik N%)` ellers, rød `⚠️ Kunne ikke bestemme — velg manuelt` hvis uklar. Toggle-knapper (`scan-rule-inkl`/`scan-rule-eks`) urørt — de rendret allerede ubetinget. `autoDetectScanRule`-internalsj urørt. Savedrule-mismatch-suffix bevart. **(C) Sum avviker dual comparison** i `renderScanSummary` (linje 5916–5950): leser ny `scanResults.totalEks`, beregner pctEks og pctInkl, tri-state-rendring — grønn "Sum stemmer med faktura" når begge (eller eneste tilstedeværende) ≤1%, viser begge par; gul info "Sum stemmer på X-siden (avvik Y%)" når kun én side stemmer (sannsynligvis AI-utledet totalsum, line-prisene OK); rød "Sum avviker fra faktura" når begge avviker. Bakoverkompatibel: `totalEks` mangler → `pctEks=null` → degraderes til inkl-only (gammel atferd). Pant-linjer (mva=0) er pass-through identisk i begge totaler. `calcPurchaseStats`/`pantTotalEks`/Matcher Fix V1.1/`scanRowMismatch` urørt.
- **Tidligere milestone**: Matcher Fix V1.1 (2026-05-22) — oppfølging av V1 etter real-world test. Tre problemer V1 ikke dekket: (1) Jaccard 0.5-terskelen i `scanRowMismatch` bommer på ekte avvik når navn deler mange ord (f.eks. Basil Seed-varianter deler "basil seed drink" + størrelse → overlap >0.5, ingen flagg); (2) bruker ville kunne overstyre matcheren på ALLE matchede rader, ikke bare flaggede; (3) `confirm()`-dialogen aksepterte AIs navn ordrett uten redigeringssti; (4) nye-produkt-rader manglet input-felt for navn (registrerte `row.raw` ordrett). Løsning — nytt optional rad-felt **`userName`** (AIs original bevart i `row.raw` = audit trail, brukers redigering i `row.userName`): display + registrering bruker `row.userName||row.raw`. Action-knappene `✏️`/`➕` rendres nå **ubetinget** på alle ikke-skippede matchede rader (gate endret fra `mismatch?` til `row.productId&&!row.isNew`) — `⚠️ NAVN AVVIK`-badge + gul tint forblir betinget på `scanRowMismatch()` (advarsel vs. verktøy). `renameMatchedProduct` bruker nå `prompt()` (prefill `userName||raw`, no-op ved cancel/tom/uendret, setter `userName` ved suksess). `treatScanRowAsNew` bruker `prompt()` for å fange brukers navn → `userName` + `guessCategory(trimmed,...)`. Nye-produkt-formen har tekst-input for navn over kategori-dropdown (`onchange→updScanRow(idx,'userName',...)`, tom → `undefined` så fallback til `raw` virker). `registerScanRowProduct` bruker `(row.userName||row.raw).trim()` som `navn`. Additivt — pant/ekskluder/skip/math/AI-prompt/`scanRowMismatch`-deteksjon urørt.
- **Tidligere milestone**: Matcher Fix V1 (2026-05-20) — bruker-overstyring av feil AI-fuzzy-matcher i faktura-skanneren. Bakgrunn: når AI ekstraherer et varenr som ikke finnes i `products`, faller matcheren tilbake til fuzzy navnematch (`matchProductByName`, `type:'maybe'`) og plukker feil eksisterende produkt — visse produkter fungerer som "fuzzy-magneter" og forurenser databasen ved hver multi-produkt-skanning. Prinsipp (Herish 2026-05-16): AI-ekstraksjon er ground truth; bruker må alltid kunne overstyre matcheren; self-healing data. Ny helper `scanRowMismatch(row)` flagger en rad når den er fuzzy-matchet (`matchType==='maybe'`) OG Jaccard token-overlap mellom AI-navn (`row.raw`) og produktnavn < 0.5. Flaggede rader får gul tint (`row-mismatch`) + `⚠️ NAVN AVVIK`-badge + to action-knapper: `✏️` `renameMatchedProduct(idx)` (confirm → `dbUpdate('products',id,{navn:aiNavn})` → setter `matchType='exact'` slik at flagget klarerer) og `➕` `treatScanRowAsNew(idx)` (confirm → bryter matchen: `productId=''`, `isNew=true`, `matchType='none'`, `newCategory=guessCategory(...)` → raden viser den eksisterende inline-registreringsformen). Begge re-renderer raden umiddelbart. Additivt: pant/ekskluder/skip-knapper og AI-prompt/ekstraksjon urørt. "Velg annen match" utsatt til V1.1.
- **Tidligere milestone**: Bank Scanner V1 Session 1.6 (2026-05-16) — SSE streaming for AI-skanner for å unngå Cloudflare 524 (100s subrequest timeout som rammet Bank Scanner Session 1 real-world test 2026-05-12 ved 2m07s). `runAIScan` (linje 4814) erstatter `await resp.json()` med SSE-parser: leser `resp.body.getReader()`, dekoder chunks med `TextDecoder('utf-8')`, line-splitter (beholder ufullstendig siste linje i buffer mellom chunks), akkumulerer `content_block_delta` med `delta.type:'text_delta'` til samlet string, fanger `stop_reason`/`usage` fra `message_delta`. Reassemblerer til `{content:[{type:'text',text:assembled}], stop_reason, usage}` slik at `showScanResults`/`showZRapportResults`/`showBankStatementResults` virker uendret (transport-agnostic downstream). **Worker forcer `stream:true` på Anthropic-kallet** (separat Cloudflare dashboard-oppdatering — Worker er single source of streaming control, Sormena holdes transport-agnostisk; deploy-gap-safe: gammel Worker + ny Sormena = soft fail med tom respons + "Ingen JSON i svar", ikke 500-crash). Errors: non-OK responses fortsatt parsed som JSON (Worker-feilpath uendret); midt-stream-avbrudd kaster `'Stream-feil: ... | Mottatt så langt: N tegn'`; malformerte enkelt-SSE-events skippes stille for å holde stream i live; defensive trailing-buffer-håndtering etter `done` (Anthropic SSE ender normalt med `\n\n`, men cheap insurance).
- **Tidligere milestone**: Pant Handling V1 (2026-05-15) — ny optional `lineKind: 'product'|'pant'` per linje på `purchases.lines[]`. Pant er pass-through-penger (Four Season betaler 2-3 kr pant til leverandør, kunde betaler samme tilbake) og må ikke distortere GM. AI-skanner auto-detekterer pant fra beskrivelse ("Pant"/"Emballasje"/"Deposit"/"Pantebeholder") og tvinger `mva:0`. Math engine: tre filter-sites (`calcPurchaseStats` 1110, `updateKalkulator` 1707, `rapKategorier` 4055) ekskluderer både `lineKind==='pant'` OG `ekskluderFraBeregning===true`. Faktura totalt (`buyEksTotal`/`buyInklTotal`) summerer ALLE linjer including pant — paper-faktura sakrosankt. Nye returverdier `pantTotalEks`/`pantTotalInkl`. Mutuell eksklusivitet mellom Pant og Ekskluder enforced i UI-handlere (`setPLineKind`/`setPLineEkskluder` i renderPLines, `updScanRow` i scanner, samme i edit/add-line modaler). Visuelt: blå tint + PANT-badge (vs gråt for Ekskluder). Bakoverkompatibel: linjer uten `lineKind` behandles som `'product'` (strict `==='pant'`-sjekk). CSV-eksport har nå `Linjetype`-kolonne. Scanner-knapp `🔄` per rad ved siden av `⊘`. Regex-fallback `/pant|emballasje|deposit|pantebehold/i` i `showScanResults` hvis AI glemmer flagget.
- **Tidligere milestone**: Bank Scanner V1 Session 1.5 (2026-05-12) — safety hardening etter Session 1 real-world test (Telenor-refusjon ble feilklassifisert som utgående pga semantisk inferens fra leverandørnavn). **Direction-regel hardet** til absolutt kolonne-basert: AI bestemmer `direction` UTELUKKENDE fra hvilken kolonne (Ut/Inn/Beløp) på utskriften beløpet står i. ALDRI fra leverandørnavn, beskrivelse eller intuisjon. Hvis kolonneplassering tvetydig: `direction:"unknown", belop:0`. **Balance extraction**: AI ekstraherer nå `startsaldo` + `sluttsaldo` til schema-toppen (null hvis ikke synlig). **Reconciliation guard** i `showBankStatementResults` (linje 5152): regner ut `startsaldo + Inn − Ut`, sammenligner mot stated `sluttsaldo` med tolerance ±1.00 NOK. Tre tilstander: ✓ Stemmer (grønn) → import-knapp aktiv; ⚠ Avvik (rød) → import-knapp blokkert, krever override-confirm; ⚠ Saldoer ikke synlig (oransje) → advarsel synlig men import går. Override-path: `confirmBankStatementOverride` (linje 5305) viser modal-confirm med konkrete konsekvenser før proxy-kall til `confirmBankStatementImport`. Linje 4839 også presisert: saldorader skal IKKE inkluderes i transaksjoner-arrayet (de hører til startsaldo/sluttsaldo-feltene).
- **Tidligere milestone**: Bank Scanner V1 Session 1 (2026-05-10) — AI-skanner for kontoutskrift. Tre scanner-modes (`faktura`/`z-rapport`/`bank_statement`). Innboks-knapp + stacked-card-display + bulk save til `bankTransactions` med Strategy B dedup. 4 nye opt-felter: `bankReference`, `importBatchId`, `rawDescription`, `aiExtracted`. Placeholder `type:'supplier_payment'`. INGEN matching i Session 1.
- **Tidligere milestone**: V0.5 forfallsdato (2026-05-08) — top-level `forfallsdato` på `purchases`. AI-skanner ekstraherer. `getInvoicePaymentStatus` utvidet med `isOverdue`/`daysOverdue`.
- **Tidligere milestone**: Ekskluder fra beregning V1 (2026-05-07) — boolean flagg `ekskluderFraBeregning` per linje på `purchases.lines[]`. Handler for IFCO, kreditnota-korreksjoner, returer (pant flyttet til dedikert Pant Handling V1, 2026-05-15). Faktura totalt sakrosankt; profit/GM mot `inkludertEks`.
- **Tidligere milestone**: STEP 1 av DNB-skanner-bygget (2026-04-26) — `payments` → `bankTransactions`-migrasjon. `payments` er cold archive.
- **Tidligere milestone**: v27 ble cutoveret til produksjon i kommit `95e308d` (2026-04-26).

**Språkpolicy (parked decision #9)**: Som av 2026-04-26: Engelsk for all ny kode (feltnavn, funksjonsnavn, identifiers). Eksisterende norske feltnavn (`dato`, `belop`, `linkedeFakturaer`, etc.) beholdes bakoverkompatibelt inntil en dedikert språkmigrasjon. Nye norske feltnavn (f.eks. `forfallsdato`) brukes kun når de matcher eksisterende norske naboer på samme record. Bank scanner Session 1 introduserer engelske felt: `bankReference`, `importBatchId`, `rawDescription`, `aiExtracted` — alle på `bankTransactions` (som allerede er hybrid). UI-strenger forblir norske.

Linjenumre er ferske per 2026-05-16 (etter Bank Scanner V1 Session 1.6). Filen vokser fort, så verifiser med `Grep` før du redigerer rundt en gitt linje.

---

## Arkitektur i ett blikk
- **Én fil**, ingen build, ingen tester. HTML + CSS + JS inline.
- **Datalag**: Firebase v8 compat. **12 Firestore-kolleksjoner totalt** — 11 mirroret til `LOCAL`-objektet (line ~901), pluss `_meta` som er system-only (migrasjons-sentinels) og IKKE i `cols`-listener. Listener pr. data-kolleksjon i `startListeners()` (line ~969). `dbAdd/dbUpdate/dbDelete/dbFind/dbAll` (line ~1027–1042) er eneste tilgang for data-kolleksjoner.
- **Sider**: 10 stk, bytte via `goTo(pg)` → `renderPage(pg)` (line ~1156). Hver render rebuildes fra `LOCAL` ved hvert kall.
- **Auth**: PIN-basert mot hardkodet `USERS`-map (line 4302). Lagres i `sessionStorage.fs_user`. Roller: `admin`, `regnskap`, `staff`. Firestore-rules er åpne — kun klientside-restriksjoner via `applyRoleRestrictions()`.
- **Boot**: IIFE `boot()` (line ~4415) hardkoder Firebase-config og hopper over setup-skjermen. `showApp()` kaller `migratePaymentsToBankTransactions()` (fire-and-forget) før UI vises.

## Konstanter
- `CATS` (line 878): 19 produktkategorier med navn, emoji, CSS-klasse.
- `ARSAK` (line 899): svinn-årsaker.
- `SUSOFT_CAT_MAP` (line 4485) + `normalizeSusoftCategory()` (line ~4506): mapper Susofts rare kategorinavn ("Mat3", "Grønnskar", "None-food7") til interne `CATS`-nøkler.
- `USERS` (line 4302): 4 brukere — Herish (admin), Admin (admin), Regnskap (regnskap), Sormena (staff). PIN-koder hardkodet. **`displayUserName(name)` (line ~4310)** mapper `'Herish'` → `'Hasher'` for visning. Underliggende navn `'Herish'` er uendret i sessionStorage, PIN-tabell og `opprettetAv`-felter.
- `AVG_ARBEIDSGIVER` 14,1%, `AVG_FERIEPENGER` 10,2%, sum 24,3% (line ~3395) — brukt i lønnskost.

## Sider (bunnav-orden)

| # | UI-navn | Render-funksjon | Page ID | Linje | Bruker kolleksjon(er) | Status |
|---|---------|----------------|---------|-------|------------------------|--------|
| 1 | Oversikt | `renderOversikt` | `pg-oversikt` | 1242 | `purchases`, `svinn`, `products`, `vakter`, `ansatte` | OK |
| 2 | Produkter | `renderProdukter` | `pg-produkter` | 1330 | `products` | OK |
| 3 | Lev. (Leverandører) | `renderLeverandorer` | `pg-leverandorer` | 2737 | `leverandorer`, `products`, `purchases` | OK |
| 4 | Innboks | `renderInnboks` | `pg-innboks` | 3180 | `innboks` | OK + bank statement scan trigger (V1 Session 1) |
| 5 | Innkjøp | `renderInnkjop` | `pg-innkjop` | 1540 | `purchases`, `products`, `bankTransactions` | OK + ekskluder V1 + forfallsdato V0.5 |
| 6 | Salg | `renderSalg` | `pg-salg` | 2235 | `dagsalg`, `sales`, `purchases` | OK |
| 7 | Svinn | `renderSvinn` | `pg-svinn` | 2654 | `svinn`, `products`, `purchases` | OK |
| 8 | Betalinger | `renderBetalinger` | `pg-betalinger` | 2883 | `bankTransactions`, `leverandorer`, `purchases` | OK (view + create + AI-import via bank scanner) |
| 9 | Timeliste | `renderTimeliste` | `pg-timeliste` | 3517 | `ansatte`, `vakter`, `dagsalg` | OK |
| 10 | Rapporter | `renderRapporter` | `pg-rapporter` | 3437 | alle | OK |

## Brukersidens funksjoner

### Oversikt-side (linje 1192)
- **Dagens kort**: innkjøp inkl. MVA, forventet fortjeneste, GM-panel (kr/%, påslag), lønnskost i dag + teoretisk fortjeneste etter lønn.
- **Måned-stats**: innkjøp, fortjeneste, margin %, svinn.
- **Lønnskost-kort**: ukentlig + månedlig lønnskost (med arbeidsgiveravgift + feriepenger), teoretisk netto.
- **Lav GM-varsel**: viser produkter med GM <20%, klikkbart → `showLowGmDiagnostics()` (linje 1340, v21).
- **Siste fakturaer**: 6 siste, klikkbare → invoice-detail.

### Produkter (linje 1280)
- Søk + tabs per kategori. Liste viser pris-badges (inn eks/inkl, salg eks/utpris) og GM-badge.
- Modal `ov-produkt` (linje 514): navn, aliaser (komma-sep, brukt for AI-matching), kategori, enhet, leverandør, MVA, priser (auto-syncet eks↔inkl).
- CSV-eksport.

### Innkjøp (linje 1529)
- Tabs: I dag / Denne uken / Denne måneden / Alle. Stats-kort + GM-panel pr. periode.
- Liste over fakturaer → `showInvoiceDetail(id)` (linje 1799) som viser:
  - Faktura-hode med leverandør, fakturanr, dato, **forfall-segment** (V0.5: `· Forfall: DD.MM.YYYY` etter dato — rød `⚠ Forfall:` hvis `pay.isOverdue`), **betalingsstatus-pill** (✓ Betalt / ⚠ Delvis / ● Ikke betalt) og "✏️ Rediger"-knapp.
  - **V0.5 forfall-banner** (rett før Reell-boks når `pay.isOverdue && p.forfallsdato`): rød boks med "⚠ Forfalt med X dag(er) — Forfallsdato var DD.MM.YYYY".
  - **Betalinger-seksjon**: sammendrag (betalt/utestående/antall) + liste pr. lenket betaling med dato, metode-emoji, referanse, beløp. Hvis betalingen er delt med andre fakturaer: "delt med N andre fakturaer". Leser fra `bankTransactions` med filter `type==='supplier_payment'||!type` (STEP 1).
  - **Reell-boks**: teoretisk resultat (innkjøp, forv. salg, svinnkost, GM) + reelt resultat hvis salg er knyttet (`calcInvoiceReelStats`, linje 1106). **Hvis ekskluderte linjer**: viser ekstra rader "Ekskludert fra beregning" og "Inkludert i beregning eks. MVA" mellom Innkjøp og Forv. salg, både i Teoretisk- og Reelt-resultat-boksen.
  - Produktlinjer med all kolli-info inkludert EKSKLUDERT-badge + dashed-border + opacity .65 når `ekskluderFraBeregning===true`. Rediger/slett pr. linje (`showEditLineForm` linje ~2002, `deleteInvoiceLine`, `showAddLineForm` linje ~2122).
  - Slett hele fakturaen.
- Liste-rader på siden viser betinget Forfall-chip i flex-wrap-rad: muted hvis fremtidig, oransje hvis i dag, rød `⚠ Forfalt:` hvis `pay.isOverdue` (V0.5).
- Modal `ov-innkjop` (linje 581): manuell faktura med kolli-linjer + AI-skanner-knapp. **V0.5 forfallsdato**: ny `.fr` 2-col rad under Dato/Fakturanr med `inn-forfall`-input (date) + helper "La stå tom hvis ukjent — kan settes senere". Tom verdi = `null` ved lagring. **Ekskluder V1 (2026-05-07)**: hver linje har "Ekskluder fra beregning"-checkbox under MVA-sats, med EKSKLUDERT-badge på pline-tittel og lett gråtonet bakgrunn når aktiv. Kalkulator-boks viser "Ekskludert fra beregning" og "Inkludert i beregning" når `tEkskludertInkl > 0`. `setPLineEkskluder(i,checked)` toggler flagget. Edit-line/add-line-modaler har samme checkbox.
- Edit-header-modal (`showEditHeaderForm` linje 1958): har `edith-forfall`-input under DATO med "La stå tom for å fjerne"-helper. `saveEditHeader` (linje 1998) tolker tom verdi som `null`.

### Salg (linje 2128)
- Tabs: uke/mnd/alle.
- "I dag"-kort: omsetning inkl/eks MVA, eller knapper "📷 Skann Z-rapport" / "✏️ Registrer manuelt" hvis ikke registrert.
- Periode-stats: omsetning vs. forv. salg fra innkjøp, faktisk margin.
- Avvik-forklaringer hvis differanse >20% (selger mer enn innkjøp / selger mindre).
- Daglig historikk fra `dagsalg`. Klikk → `showDagsalgDetail(id)` (linje 2386) hvis `kilde='z-rapport-skann'` eller har kategorisalg, ellers `openDagsalgModal(id)`.
- "Detaljerte salg" (line-level `sales`-kolleksjon): liste, slett. Modal `ov-salg` (linje 666) finnes, men FAB peker til `openDagsalgModal` — så detaljerte salg er reelt sett bare lesbart fra UI.

### Svinn (linje 2547)
- Total svinnkost øverst.
- Liste pr. svinnpost. Modal `ov-svinn` (linje 683): produkt, faktura-knytting (auto-foreslår nyligste faktura for frukt), antall, årsak.
- Auto-knytting i `calcInvoiceReelStats`: usrelaterte svinn for frukt/grønt innenfor ±14 dager av faktura-dato regnes med — for å unngå dobbeltelling.

### Leverandører (linje 2630)
- Liste sortert alfabetisk. Hver lev viser: produkter, innkjøp, antall aliaser, prisregel-badge (inkl/eks/spør).
- "⚠️ N navn fra produkter og innkjøp er ikke i registeret" + `migrateLeverandorer()` (linje 2753): masse-oppretter fra free-text leverandørnavn.
- Modal `ov-leverandor` (linje 766): navn, org, konto, kontaktperson, prisregel, aliaser.
- `findLeverandorByName` (linje 2620) + `normName` (linje 2618): brukes til å matche fakturalev mot register.

### Innboks (linje 3180)
**Hva den gjør (sett fra brukerens side)**: holder PDF-er og bilder av fakturaer/Z-rapporter/kontoutskrifter klare for skanning. Filer dras inn (eller velges via filplukker), lagres som base64 i `innboks`-kolleksjonen i Firestore (max 700KB pr. fil, da Firestore docs maks er 1MiB), og venter til brukeren trykker en av tre skann-knapper.
- Pending-liste viser filnavn, alder, og "💾 Utkast"-badge hvis skanning er påbegynt.
- **Tre skann-knapper per pending-rad** (V1 Session 1, 2026-05-10): "🛒 Skann som faktura", "💰 Skann som Z-rapport", "🏦 Skann som kontoutskrift". Triggers: `scanInnboksAsFaktura`/`scanInnboksAsZRapport`/`scanInnboksAsBankStatement` (linje ~3305-3307) — alle dispatcher til `openScannerFromInnboks(id, mode)` med mode-string `faktura`/`z-rapport`/`bank_statement`.
- Utkast-system: kun for faktura-mode. Z-rapport og bank statement har ikke utkast — one-shot extract.
- "Ferdig"-mark eller slett pr. fil.
- Når faktura/Z-rapport/bank statement er lagret kalles `markInnboksLinkedDone()` automatisk — setter `status:'done'`.

### Betalinger (linje 2776)
**Hva den gjør (sett fra brukerens side)**: registrer hver betaling Four Season gjør (bankoverføring, kort, giro), valgfritt lenket til én eller flere ulenkede fakturaer fra samme leverandør. Lar brukeren se hvilke fakturaer som er betalt, delvis betalt, eller venter på betaling. **STEP 1 lager grunnlag for DNB-skanner**: data går nå til `bankTransactions`-kolleksjonen, ikke `payments`. UI viser fortsatt "Betalinger" — rename til "Banktransaksjoner" kommer i STEP 2.
- Tabs: I dag / Denne uken / Denne måneden / Alle.
- Filter: per leverandør (dropdown).
- Liste pr. betaling: dato, metode-emoji (🏦 transfer / 💳 card / 🧾 giro), leverandørnavn (eller ⚠️ ikke i register), referanse, "🔗 lenket"/"ulenket"-badge, beløp. Filtreres på `type==='supplier_payment'||!type` (STEP 4 utvider med tabs for andre typer).
- Bunnen: total beløp + antall betalinger.
- Modal `ov-betaling` (linje 804): to-faset.
  - **Phase 1** (`#bet-phase-1`): dato, booking-dato, leverandør (dropdown + free-text fallback), beløp, metode, bankkonto, referanse, arkivref (DNB unik bank-ID), notat. "Neste →" går til Phase 2.
  - **Phase 2** (`#bet-phase-2`): liste over **ulenkede fakturaer** for valgt leverandør (filtrert på `status !== 'paid'`), checkboxer. Status-linje viser om beløp matcher sum av valgte (✓ match / ⚠ differanse).
- Funksjoner: `openBetalingModal()` (line 2837), `onBetLevChange()` (line 2855), `goToBetPhase2()` (line 2866), `goToBetPhase1()` (line 2880), `renderBetPhase2Invoices()` (line 2885), `toggleBetInvoice()` (line 2924), `updateBetLinkStatus()` (line 2932), `saveBetaling()` (line 2950) — duplikat-sjekk på `arkivref` mot `bankTransactions` (alle typer, ikke bare supplier_payment) før lagring. Skriver `type:'supplier_payment'`, `direction:'out'`, `period` (YYYY-MM) i tillegg til norske legacy-felter.

### Timeliste (linje 3408, v22)
- Tabs: Uke / Måned / Ansatte / Rapport.
- Uke-fane (`renderTlUke`, linje 3428): 7 dager, vakter pr. dag med fra/til eller direkte timer, "+ Legg til vakt" pr. dag.
- Måned-fane (`renderTlManned`, linje 3488): per ansatt — timer, brutto, full kost. CSV + PDF-eksport.
- Ansatte-fane (`renderTlAnsatte`, linje 3544): personalia, masker personnummer ("vis"-knapp).
- Rapport-fane (`renderTlRapport`, linje 3582): per ansatt, top 5 dager, lønnsandel mot omsetning.
- Modaler: `ov-ansatt` (linje 723), `ov-vakt` (linje 744). Personnummer maskeres med `maskPersonnummer` (linje 3354).

### Rapporter (linje 3328)
- Tabs: Fakturaer, Kategorier, Produkter, Per dag, Svinn, Uke vs Uke, Graf.
- Hver tab er en separat ren-funksjon (`rapFakturaer`, `rapKategorier`, `rapProdukter`, `rapDag`, `rapSvinn`, `rapUkeVsUke`, `rapGraf` — linje 3901–4218).
- Graf-fanen er HTML-basert bar-chart, 30 dager, innkjøp + GM kr.

## Modaler (alltid i DOM)

| ID | Tittel | Linje | Brukes av |
|----|--------|-------|-----------|
| `ov-produkt` | Nytt/Rediger produkt | 514 | Produkter |
| `ov-innkjop` | Nytt innkjøp / faktura | 581 | Innkjøp + AI-skanner |
| `ov-dagsalg` | Registrer dagens salg | 610 | Salg |
| `ov-dagsalg-detail` | Dagsalg-detaljer | 653 | Salg (Z-rapport-skannede) |
| `ov-salg` | Registrer salg (linje-nivå) | 666 | Salg (skjult — kun via kode) |
| `ov-svinn` | Registrer svinn | 683 | Svinn |
| `ov-detail` | Faktura-detalj | 707 | Innkjøp |
| `ov-lowgm` | Lav GM diagnose | 715 | Oversikt-varsel (v21) |
| `ov-ansatt` | Ny/Rediger ansatt | 723 | Timeliste (v22) |
| `ov-vakt` | Legg til vakt | 744 | Timeliste (v22) |
| `ov-leverandor` | Ny/Rediger leverandør | 766 | Leverandører |
| `ov-betaling` | Ny betaling (2-fase) | 804 | Betalinger (v27) |
| `ov-utgifter-actions` | Utgifter FAB action sheet (Skann faktura / Legg til manuelt / Åpne innboks) | ~830 | Utgifter (phase 2a step 3) |
| `ov-ny-utgift` | Ny utgift — manuell driftsutgift (operating expense form) | ~845 | Utgifter (phase 2a step 3) |
| `ov-scanner` | AI-skanner | 5972 | Faktura + Z-rapport + Bank statement (V1 Session 1+1.5) |

## Kalkulasjons-motor (linje 1066–1153)
- `calcGM(buyEks, sellEks)` (linje 1066) → `{gmKr, gmPct, paaslagPct}`.
- `calcKolliLine(antall, vektPerKolli, prisPerKolliInkl, vatRate, bonusKolli)` (linje 1072) — håndterer "kjøp 20, få 10 gratis" via bonus. Uendret signatur — eksklusjons-logikk sitter på linje-objekt-nivå, ikke i denne funksjonen.
- `calcPurchaseStats(purchase)` (linje 1110) — summerer fakturaens linjer. **Faktura totalt (`buyEksTotal`/`buyInklTotal`) summerer ALLE linjer including ekskluderte OG pant** — paper-faktura-kontrakt sakrosankt. Totals: `inkludertEks/Inkl` (sum av ikke-pant + ikke-ekskluderte), `ekskludertEks/Inkl` (sum av ekskluderte, ikke-pant), `pantTotalEks/Inkl` (sum av `lineKind==='pant'` — Pant V1, 2026-05-15). `forvSalgEks` skipper både ekskluderte og pant-linjer. **GM regnet mot `inkludertEks`** (post-ekskluder/pant kost), ikke `buyEksTotal`. Prioritet i filter: pant først, deretter ekskluder, ellers inkludert — `lineKind==='pant'` vinner over `ekskluderFraBeregning`. Defensive strict-equality på begge flagg: linjer uten flaggene behandles som `lineKind:'product'` / inkludert.
- `calcInvoiceReelStats(invoiceId)` (linje 1106) — krysser med `sales` og `svinn` for reelt resultat. Auto-attribuerer frukt/grønt-svinn ±14 dager. **`reellGmKr` bruker `base.inkludertEks`** (ikke `buyEksTotal`) — ellers ville reell GM overdrive kost for fakturaer med ekskluderte linjer.
- `getInvoicePaymentStatus(invoiceId)` (linje 1143, v27 + V0.5) — leser fra `bankTransactions` med type-filter `type==='supplier_payment'||!type`. **Even-split attribusjon**: 1/N pr. lenket faktura. Returnerer `{paid, paymentIds, paidAmount, status, total, outstanding, forfallsdato, isOverdue, daysOverdue}` der `status ∈ {'paid', 'partial', 'unpaid'}`. Bruker `buyInklTotal` (paper-faktura) som total — riktig fordi du betaler hele paper-fakturaen, ekskluderte linjer er fortsatt fakturert. **V0.5 forfallsdato (2026-05-08)**: `isOverdue=true` når `forfallsdato && status!=='paid' && today() > forfallsdato`. ISO `YYYY-MM-DD`-format gjør string comparison gyldig. `daysOverdue=Math.floor((Date.parse(today)-Date.parse(forfallsdato))/86400000)` når `isOverdue`, ellers 0. Eksisterende fakturaer uten flagget: `forfallsdato=null`, `isOverdue=false`.
- `marginCls(m)` / `marginBadge(m)` (linje ~1144) — fargeskala: ≥30% grønn, ≥20% oransje, <20% rød.
- `gmPanelHtml(gmKr, gmPct, paaslagPct)` (linje ~1145) — gjenbrukbar 3-korts visning.

## Migrasjoner (linje 4385–4429)
One-shot data-transformasjoner gated av Firestore `_meta/migrations`-dokumentet. Kjøres fra `showApp()` rett før UI vises (fire-and-forget; idempotent via sentinel + per-doc `existingIds`-skip).

- `migratePaymentsToBankTransactions()` (linje ~4437, STEP 1 av DNB-skanner) — kopierer alle records fra `payments` → `bankTransactions`, beholder source-ID og alle norske felter (`...p` spread), legger til engelske `type:'supplier_payment'`, `direction:'out'`, `period` (YYYY-MM), `migratedFromPayments:true`, `migratedAt`. Sentinel-felt: `paymentsToBankTransactions` (ISO-timestamp). Ved feil: sentinel ikke satt, toast vises, retry på neste boot.

## AI-skanner (linje 4429–5624)

### Faktura-skanner
- Inngang: `openScanner('faktura')` (linje 4483) eller via knapp i innkjøp-modal eller "Skann som faktura" i Innboks (`scanInnboksAsFaktura`, linje 3198).
- Multi-fil støtte: opp til 10 filer, 20MB total — alle blir én faktura.
- Sender til Cloudflare Worker `https://fourseason.herishhashemi.workers.dev` med `claude-sonnet-4-6`. API-nøkkel i `localStorage.fs_anthropic_key` (`saveApiKey`, linje 4471).
- Prompt definert i `runAIScan()` (linje 4814), legger ved bilder/PDF som `image`/`document`-content-blokker. **3 prompt-grener** (mode-dispatch via `scanMode`-variabel): `faktura` (default), `z-rapport`, `bank_statement` (V1 Session 1, 2026-05-10). **V0.5 (2026-05-08)**: faktura-schema utvidet med `forfallsdato:"YYYY-MM-DD eller null"`. Extraction-regler: explicit "Forfall"/"Forfallsdato"/"Due date" → bruk direkte; "Betalingsfrist N dager"/"N dager netto"/"Net N days"/"Betalingsbetingelser N dager" → kalkuler `dato + N`; ingenting synlig → `null`. Eksisterende kontrollsum-regel (sum av linjer = sum eks. MVA) bevart urørt. **Math Verification V1 (2026-05-23)**: faktura-schema utvidet med `totalEks:0.00` + optional `mvaBreakdown:{"15":{grunnlag,mva},"25":{...}}` (samme shape som z-rapports `mvaBreakdown` linje 4846). Ny MATH-VERIFY-regel etter eksisterende KONTROLL: krever literal celle-extraction (aldri utled `totalInkl`/`totalEks`), AI-side selv-sjekk sum(linjer)≈totalEks (5 kr eller 0,5%) + grunnlag×(1+S/100)≈grunnlag+mva pr sats (1 kr), revurder linjetolkning **før retur** hvis ikke stemmer, eksplisitt Sunrise/AFFA/Tenza-eksempel om 15%-MVA-felle.
- **Session 1.6 streaming (2026-05-16)**: respons fra Cloudflare Worker leses som SSE (`text/event-stream`), parses event-for-event med `resp.body.getReader()` + `TextDecoder`, reassembleres til `{content:[{type:'text',text:assembled}], stop_reason, usage}`. Worker forcer `stream:true` på Anthropic-kallet (single source of streaming control). Fix for Cloudflare 524 ved skanninger som tok >100s (rammet real-world bank statement-test 2026-05-12 ved 2m07s). Downstream parsere uendret — kun transport-laget endret. Non-OK responses parses fortsatt som JSON.
- `autoDetectScanRule(scanData)` (linje 5468, v25): regner ut om priser er eks/inkl. MVA basert på fakturatotal vs. linjesum. Matematikken slår lagret leverandør-regel når avvik <5%. **Math Verification V1 (2026-05-23)** rører ikke denne funksjonen — kun label-rendringen som konsumerer dens output (`showScanResults` linje 5427 starter nå alltid med "🧮 Auto-regel:" + severity-suffix). Self-consistency-svakhet (AI internt konsistent men feil i eks/inkl-tolkning → autodetect plukker feil regel med lav avvik) er adressert i AI-prompten via MATH-VERIFY-regelen som krever literal printed `totalInkl`/`totalEks`-extraction og AI-side selv-sjekk før retur.
- `showScanResults` (linje 5310) → tabell med 16 kolonner. `scanRows` har all state. Bruker kan justere antall, innhold/kolli, MVA, utsalgspris pr. linje. Hver scanRow har `skipped` (hopp over helt), **`ekskluder`** (Ekskluder V1, 2026-05-07), og **`lineKind`** (`'product'|'pant'` — Pant V1, 2026-05-15). Action-cell har tre faste knapper: `🔄` (pant-toggle, blå), `⊘` (ekskluder-toggle, grå), `×` (skip), pluss `✏️`/`➕`-knapper (Matcher Fix V1, 2026-05-20; V1.1 2026-05-22) som siden V1.1 vises på ALLE ikke-skippede matchede rader (`row.productId&&!row.isNew`), ikke bare flaggede. Skipped-rader når aldri pLines (`confirmScanToInnkjop`); ekskluderte og pant-rader når pLines med respektive flagg. Mutuell eksklusivitet i `updScanRow` (linje 5646): setter pant → fjerner ekskluder + tvinger mva=0; setter ekskluder → fjerner pant. Regex-fallback `/pant|emballasje|deposit|pantebehold/i` på beskrivelse i row-build hvis AI glemmer å sette flagget. **Matcher Fix V1/V1.1**: `scanRowMismatch(row)` (helper nær `matchProductByName`) flagger fuzzy-matchede rader (`matchType==='maybe'`) der Jaccard token-overlap mellom `row.raw` og produktnavn < 0.5 → gul `row-mismatch`-tint + `⚠️ NAVN AVVIK`-badge (kun advarsel; gater ikke knapper siden V1.1). `renameMatchedProduct(idx)` og `treatScanRowAsNew(idx)` ligger rett etter `registerScanRowProduct` — begge bruker siden V1.1 `prompt()` (prefill `row.userName||row.raw`) i stedet for `confirm()`, og lagrer brukers navn i optional rad-felt `userName`. Nye-produkt-rader har tekst-input for navn; `registerScanRowProduct` og all visning bruker `row.userName||row.raw`. `renderScanSummary` (linje ~5716) viser eksplisitt "Ekskludert fra beregning" og "Inkludert i beregning"-rader når noen linjer er ekskludert.
- Inline produktregistrering for ukjente produkter (`registerScanRowProduct`, linje 5361) — det finnes ingen separat "produktskanner"; dette er produktskanneren.
- **Sum avviker kontrollsum** i `renderScanSummary` (linje ~5916, Math Verification V1, 2026-05-23): dual eks+inkl-sammenligning. Leser `scanResults.totalEks` (ny) og `scanResults.totalInkl` (eksisterende), beregner pctEks og pctInkl uavhengig. Tri-state-rendring: (1) **grønn "Sum stemmer med faktura"** når begge (eller eneste tilstedeværende) ≤1% — viser begge par "Eks. MVA: X mot Y" + "Inkl. MVA: X mot Y"; (2) **gul info "Sum stemmer på X-siden"** når begge totaler tilstede MEN kun én side stemmer — line-prisene tolkes som korrekte (den andre siden sannsynligvis AI-utledet); (3) **rød "Sum avviker fra faktura"** når begge avviker eller eneste tilstedeværende totalt avviker. Bakoverkompatibel: `totalEks` mangler ⇒ `pctEks=null` ⇒ inkl-only (gammel atferd). Pant-linjer (mva=0) er pass-through identisk i begge totaler.
- Duplikat-sjekk på fakturanr + lev (i `runAIScan`, v22): tidlig avbryt hvis faktura allerede finnes.
- Lagring av utkast (`saveScanAsDraft`, linje 3253): hvis filen ikke ligger i Innboks, tilbyr å først lagre der.
- `confirmScanToInnkjop` (linje 5530) overfører til `pLines` og åpner `ov-innkjop`-modalen.
- **Lagres til**: `purchases`-kolleksjonen som vanlig faktura. Innboks-fil markeres `done`. Produkter får oppdatert `buyEks`/`buyInkl` automatisk (v21 Fix 2). Hvis utsalgspriser er endret: tilbyr å oppdatere `salgEks`/`salgInkl` på produktene.

### Z-rapport-skanner
- Inngang: `openZRapportScanner()` (linje 4627) — kalles fra "📷 Skann Z-rapport"-knapp på Salg-siden, eller "Skann som Z-rapport" i Innboks (`scanInnboksAsZRapport`).
- Bruker samme `runAIScan()` med `scanMode='z-rapport'` — egen prompt som ber om totalbeløp, MVA-fordeling, kategorisalg, ordretall, rabatt/uttak/slettet/retur.
- `showZRapportResults` (linje 4982) → forhåndsvisning med MVA-tabell + kategorisalg (med Susoft→intern matching) + meta.
- `confirmZRapportScan` (linje 5074) lagrer/oppdaterer i `dagsalg`-kolleksjonen med `kilde:'z-rapport-skann'`. Felter: `mvaBreakdown`, `kategoriSalg[]` med `internalKey` for matchede kategorier, `antallOrdre/Kvittering`, `rabattTotal`, `vareuttak`, `slettetTotal`, `retur`.
- **Lagres til**: `dagsalg`-kolleksjonen — vises på Salg-siden. Detalj-visning (`showDagsalgDetail`) viser MVA-fordeling og kategorisalg som tabell.

### Bank statement-skanner (V1 Session 1+1.5, 2026-05-10/12)
- Inngang: `openBankStatementScanner()` (linje 4628) — kalles fra "🏦 Skann som kontoutskrift" i Innboks (`scanInnboksAsBankStatement` linje ~3307).
- Bruker samme `runAIScan()` med `scanMode='bank_statement'` — egen prompt som ber om kontonummer/eier/periode + **startsaldo/sluttsaldo** (V1.5) + transaksjoner-array (`{dato, bookingDato, belop signert, direction, beskrivelse, bankReference, motpart}`). Prompt har eksplisitt error-handling: hvis PDF ikke lesbar, returnér `{"transaksjoner":[],"feil":"PDF kunne ikke leses"}`.
- **V1.5 absolutt direction-regel**: prompt forbyr eksplisitt å utlede `direction` fra leverandørnavn, beskrivelse eller intuisjon. Direction skal kun bestemmes av kolonne-plassering (Ut/Inn/signert Beløp). Tvetydig kolonne → `direction:"unknown", belop:0`. Innført etter Session 1-feil der Telenor-refusjon ble feilklassifisert.
- `showBankStatementResults` (linje 5152) → stacked-card-liste med Inn/Ut-totaler, dup-deteksjon (sjekker `arkivref || bankReference` mot eksisterende records — Strategy B for kryss-kanal-beskyttelse), big "💾 Importer N transaksjoner"-knapp. Fanger `data.feil` før liste-rendering.
- **V1.5 reconciliation guard** i `showBankStatementResults`: når `startsaldo` og `sluttsaldo` er ekstrahert, regner ut `computed = startsaldo + totalIn − totalOut` og sammenligner mot stated `sluttsaldo` med tolerance ±1.00 NOK. Tre tilstander vist som banner over import-knappen: (1) ✓ Stemmer (grønn boks med beregning) → `canImport=true`; (2) ⚠ AVVIK (rød boks med diff-beløp) → `canImport=false`, knapp grå "⚠ Import blokkert — krever bekreftelse" som kaller `confirmBankStatementOverride` (linje 5305) — vises full-text confirm-dialog med konsekvenser før proxy-kall til `confirmBankStatementImport`; (3) ⚠ Saldoer ikke synlig (oransje boks) → `canImport=true`, advarsel om manuell sjekk men ikke blokkert.
- `confirmBankStatementImport` (linje 5244) lagrer alle ikke-duplikate transaksjoner til `bankTransactions` via async loop. Setter `aiExtracted:true`, `importBatchId` (én per import), `rawDescription`, `bankReference` (= AI-ekstrahert ref, også speilet til `arkivref`-feltet for cross-channel-dedup). Placeholder `type:'supplier_payment'`, `linkedeFakturaer:[]`. Reklassifisering + matching kommer i Session 2.
- **Lagres til**: `bankTransactions`-kolleksjonen — synlig på Betalinger-siden. Innboks-fil markeres `done`. Etter import navigeres bruker til Betalinger-siden via `goTo('betalinger')`.
- **Worker-pass-through**: Cloudflare Worker (`fourseason.herishhashemi.workers.dev`) er antatt å passere prompten til Anthropic uendret. Ikke 100% verifiserbar uten Worker-kode (separat repo). Hvis Worker-en avviser bank_statement-prompt: feilmelding vises i scanner-UI via `showScanError`.

## Datafelter — hva ligger i hver Firestore-kolleksjon

| Kolleksjon | Lagrer hva | Sentrale felter |
|------------|-----------|-----------------|
| `products` | Produktregister | `navn`, `kategori`, `enhet`, `leverandorId`, `lev` (legacy text), `mva`, `aliaser[]`, `buyEks`, `buyInkl`, `salgEks`, `salgInkl`, `aktiv` |
| `purchases` | Fakturaer/innkjøp | `dato`, **`forfallsdato`** (string YYYY-MM-DD eller null — V0.5, 2026-05-08), `fakturanr`, `leverandorId`, `lev`, `lines[]` (med `productId`, `antall`, `vektPerKolli`/`innholdPerKolli`, `prisPerKolliInkl`, `vatRate`, `bonusKolli`, **`ekskluderFraBeregning`** (boolean, default false — Ekskluder V1, 2026-05-07), **`lineKind`** (`'product'\|'pant'`, default `'product'` — Pant V1, 2026-05-15), kalkulerte `totalEks`/`totalInkl`/`prPerEnhetEks`/`prPerEnhetInkl`/`totalVekt`/`effAntall`), `notes`. **V0.5 forfallsdato**: top-level field. AI-skanner ekstraherer fra explicit "Forfall"-felt eller kalkulerer fra "Betalingsfrist N dager + dato". Returneres som `null` hvis ikke synlig på fakturaen. Brukes av `getInvoicePaymentStatus` for `isOverdue/daysOverdue`-felt. **Ekskluder V1**: linjer med `ekskluderFraBeregning===true` lagres fullt på fakturaen (audit trail) men telles ikke i profitt/GM-math. **Pant V1**: linjer med `lineKind==='pant'` lagres med `vatRate:0` (norsk skattelov) og telles ikke i GM (pass-through-penger), men inkluderes i `buyEksTotal`/`buyInklTotal` (faktura totalt). Mutuell eksklusivitet mellom pant og ekskluder enforced i UI-handlere. Faktura totalt summerer ALLE linjer including ekskluderte OG pant — paper-faktura-kontrakt. Eksisterende fakturaer uten flaggene behandles som `lineKind:'product'` / `ekskluder:false` (strict `===`-sjekk). |
| `sales` | Linje-nivå salg (manuell) | `dato`, `ref`, `fakturaId` (link), `lines[]` med `productId`, `qty`, `sellPriceEks` |
| `dagsalg` | Daglig salgs-total fra POS | `dato`, `totalInkl`, `totalEks`, `mva`, `kilde`, `antallTrans`, `ref`, + Z-skann: `nummer`, `klokke`, `mvaTotal`, `mvaBreakdown`, `kategoriSalg[]`, `antallOrdre`, `rabattTotal`, `vareuttak`, `slettetTotal`, `retur` |
| `svinn` | Svinn-poster | `dato`, `productId`, `qty`, `arsak`, `komm`, `fakturaId` (eksplisitt link), `kostverdi` |
| `leverandorer` | Leverandørregister | `navn`, `aliaser[]`, `orgNummer`, `kontoNummer`, `kontaktperson`, `telefon`, `epost`, `prisregel` (`ask`/`inkl`/`eks`), `notat`, `fraMigrasjon` |
| `innboks` | Filer som venter på skanning | `filnavn`, `storrelse`, `type` (`pdf`/`image`), `mimeType`, `data` (base64), `status` (`pending`/`done`), `notat`, `utkast` (`{scanResults, scanRows, savedAt}`) |
| `ansatte` | Ansatte (v22) | `navn`, `stilling`, `timelonn`, `adresse`, `epost`, `bankkonto`, `personnummer`, `notater`, `aktiv` |
| `vakter` | Vakter (v22) | `dato`, `ansattId`, `fra`, `til`, `timer` (alternativ til fra/til), `notat` |
| `payments` | **Cold archive** — replaced by `bankTransactions` via migration 2026-04-26 (STEP 1 av DNB-skanner). **Do not write.** Beholdes urørt for rollback-mulighet. | (uendret skjema fra v27): `tenantId:'fourseason'`, `dato`, `bookingDato`, `leverandorId`, `levTekstRaa`, `belop`, `valuta:'NOK'`, `bankkonto`, `referanse`, `arkivref` (DNB unik), `metode` (`transfer`/`card`/`giro`), `linkedeFakturaer[]`, `opprettetAv`, `opprettet`, `notat` |
| `bankTransactions` | Banktransaksjoner — kun `supplier_payment` i Session 1, utvides med lønn/drift/POS-innskudd i Session 2-4 | **Mixed Norwegian (legacy from `payments`) and English (new) field names. Full English migration parked as decision #9.** Legacy NO felter: `tenantId`, `dato`, `bookingDato`, `leverandorId`, `levTekstRaa`, `belop`, `valuta`, `bankkonto`, `referanse`, `arkivref`, `metode`, `linkedeFakturaer[]`, `opprettetAv`, `opprettet`, `notat`. EN felter (V0.4 STEP 1): `type` (i dag bare `'supplier_payment'`), `direction` (`'in'`/`'out'`), `period` (YYYY-MM derived fra dato). Migration-only marker: `migratedFromPayments:true`, `migratedAt` (ISO-timestamp). **V1 Session 1 (2026-05-10) AI-skanner felter** (kun på AI-importerte records): `bankReference` (samme verdi som `arkivref` — Strategy B speiling for cross-channel dedup), `importBatchId` (én ID per skann-import-batch), `rawDescription` (uendret tekst-felt fra kontoutskriften), `aiExtracted:true`. Manuelt registrerte records har disse 4 felter `undefined` (defensive: alle dedup-/filter-sjekker bruker `||` eller `===true`). |
| `_meta` | System-metadata. **IKKE i `cols`-listener** — leses on-demand fra Firestore. | I dag bare `migrations`-doc med felt `paymentsToBankTransactions: <ISO-timestamp>` (sentinel for STEP 1). Fremtidige migrasjoner legger til flere felter i samme doc. |

## Kjente "skjermer/funksjoner uten tydelig formål" eller halvferdige

- **`ov-salg`-modal (linje 666)** — line-level salgsregistrering. Eksisterer men er **ikke koblet til FAB eller hdr-knapp** — FAB på Salg-siden går til `openDagsalgModal`. Bare lesbar fra UI ("Detaljerte salg"-listen viser `sales`, og `openSalgModal` finnes i kode (linje 2493) men kalles ikke fra noe synlig). Sannsynlig brukt av AI-skanner-flyt eller eldre integrasjon. **Vurder å avgjøre: enten gjenbruk eller fjern.**
- **`#setup-screen`** (~linje 340) — onboarding-UI for Firebase-config. Skjules ved boot fordi `boot()` hardkoder config. Kun nåbar via `resetSetup()`. Kan trolig fjernes.
- **`exportTimelistePDF`** (linje 3812) åpner ny vindu og kaller `print()`. Funksjonell, men avhenger av at popups ikke blokkeres.
- **Betalinger har bare create**, ingen `editBetaling`/`deleteBetaling` — listen er view-only. Hvis bruker oppdager feil, må de slette betalingen direkte fra Firestore.
- **Even-split attribusjon for betalinger**: 1/N pr. lenket faktura. Bevisst forenkling — kommentert i koden som "raffineres i STEP 5 hvis accountant trenger per-invoice splits".
- **Ingen bank-import enda** — alle betalinger skrives manuelt. STEP 4 av DNB-skanner-bygget vil legge til kontoutskrift-skanning til `bankTransactions`.
- **Hybrid skjema i `bankTransactions`**: blanding av norske legacy-felter og engelske nye felter. Full språk-migrasjon parked som decision #9.
- **Ingen rapport-aggregering** for `bankTransactions`-kolleksjonen — Rapporter-fanen dekker ikke betalinger.
- **Ingen forfalls-/reminder-tracking** på fakturaer.
- Ingen e-post-/varsels-system. Ingen offline-støtte (kun Firestore-cache via SDK-en).

## Andre praktiske detaljer
- **Sync-indikator**: liten grønn dot i header. `setSyncing()` på alle writes, `setSynced()` på snapshots.
- **Toast**: `showToast(msg, 'ok'|'err')`.
- **CSV-eksport**: `exportCSV('products'|'purchases')` — header-knapp på de to sidene.
- **Norske ID-er overalt**: alle UI-strenger på norsk, datoer YYYY-MM-DD intern, DD.MM.YYYY visning, NOK med komma-desimal.
- **Engelsk for ny kode** (parked decision #9): nye feltnavn, funksjonsnavn, identifiers er engelske. Eksisterende norske felter rørres ikke.

---

## Hvordan bruke dette dokumentet

- **Når Soren planlegger ny funksjonalitet**: sjekk dokumentet først for å se om noe lignende allerede finnes.
- **Når Claude Code skal endre kode**: bruk linjenumrene som inngangspunkt, men verifiser med `Grep` — filen vokser fort og linjene driver.

Dokumentet bør oppdateres når:
- En ny side, modal eller kolleksjon legges til.
- En eksisterende modul får vesentlig ny oppførsel.
- En halvferdig funksjon enten ferdigstilles eller fjernes.
